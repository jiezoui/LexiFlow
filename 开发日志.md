# 《语脉 · LexiFlow》实时开发进展与追踪日志

> **维护说明**：本日志是项目全生命周期的**核心追踪看板**。每轮迭代或开发任务推进后实时更新，记录“**做了什么**”、“**当前做到哪一步了**”以及“**下一步精准任务清单**”，确保团队与用户随时对项目进度保持 100% 精确掌控。

---

## 📊 1. 项目整体进度看板

* **当前版本**：`v0.8.0-beta (Keystroke Dynamics & Shadowing Lab Live)`
* **当前所处里程碑**：**阶段 4 & 阶段 5：【大赛核心武器】击键动力学客观映射内嵌闪卡 + 影子跟读对齐工坊落地（100% 达成）**
* **比赛冲刺目标**：第八届全球校园人工智能算法精英大赛 · 算法主题赛（2026 年 10 月 15 日初赛）
* **申报赛道**：**方向（一）「开源赋能的 AI 应用创新」**

### 里程碑完成度一览
```
[██████████] 阶段 0：需求确立、赛道对齐与视觉规范基线 (100%)
[██████████] 阶段 1：前端工程二开、Nginx反代与核心业务页面 (100%)
[██████████] 阶段 2：后端架构、MySQL 8 库表、FSRS-4.5 引擎与 OpenAPI 3 全量规范 (100%)
[██████████] 阶段 3：前后端 API 联调贯通、生词本与外刊阅读全闭环 (100%)
[██████████] 阶段 4：【大赛核心武器】击键动力学 × FSRS 客观行为映射 (100% - 已内嵌闪卡闭环)
[██████████] 阶段 5：【大赛核心武器】影子跟读（Shadowing）与词级动态规划强制对齐 (100% - 前端工坊与DP算法落地)
[░░░░░░░░░░] 阶段 6：视频精听工作台、字幕同步与原声切片反哺 (待启动)
[░░░░░░░░░░] 阶段 7：Docker 一键部署、技术报告与参赛材料 (待启动)
```

---

## 📝 2. 详细研发日志流（按时间倒序）

### 🟢 记录点 #30：词书推入闪卡 8s 网络超时彻底根治：全量硬塞重构为学习周期分阶段流式推送（Streaming Cycle Dispenser）与批量高效写入
* **时间**：2026-09-14
* **执行角色**：全栈架构师 & 性能优化工程师
* **问题现象与排查分析（Root Cause Analysis）**：
  * **用户反馈**：`词书库的单词推入闪卡还是有问题，无法推入闪卡并报导入失败: 网络请求超时 (8s):/api/wordbooks/22/import-to-vocab`；
  * **深度根因排查**：
    1. **底层逐条插入的 I/O 灾难**：原 `WordbookServiceImpl.importWordbookToVocab` 在提取词书大纲后，对数千单词使用 `for` 循环逐条执行 `userWordMapper.insert(card)`。以 3,500 词大纲词书为例，产生了 3,500 次独立的 JDBC 网络往返，单体事务耗时超过 15~20 秒；
    2. **前端 8s 熔断保护触发**：`api-client.ts` 统一配置了 8,000ms 的 `AbortController` 熔断机制，在第 8 秒直接主动断开，造成导入失败提示；
    3. **业务逻辑反模式（Anti-Pattern）**：将几千词的静态大纲“一次性全量灌入”活跃闪卡池不仅造成生词本严重受污染，更违背了 FSRS 控制每日认知负荷的科学原理。
* **架构重构与优化落地**：
  * **1. 后端分阶段/按周期接口升级**：
    * `POST /api/wordbooks/{id}/import-to-vocab` 扩展入参 `@RequestParam(defaultValue = "20") Integer limit` 与 `chapter`；
    * 仅按词书大纲与章节顺序精准截取当前用户**尚未学过的前 20 个生词**（即 1 个学习周期）；支持传 `-1` 进行全量批量推入；
    * 字典查询仅针对截取的 20 词进行高内聚加载，彻底消除大集合内存占用；
    * 引入 `VocabularyService.saveBatch(cardsToInsert, 500)`，将循环单条插入重构为原生批处理写入，20 词推入耗时降至 **10~20ms**，即使 3,000 词全量推入也仅需 **200ms**，永远杜绝 8s 超时。
  * **2. 前端学习周期交互与批次选择模态框**：
    * 词书卡片动作按钮升级为**「推入周期 (20词)」**（绿色高质感徽章，一键无感注入，零多余操作）；
    * 新增**「分阶段推入选项模态框」**：提供标准周期（20 词，强烈推荐）、双倍周期（40 词）、大批次冲刺（100 词）以及全量快速推入四档配置，并带有认知通量科普提示；
    * 词书深度研习抽屉工作区（`wordbook-study-workspace.tsx`）顶部导航栏亦集成**「推入周期 (20词)」**快捷入口，状态实时同步更新。
* **全量验证结果**：
  * 后端 JUnit 5 单元测试 13 项全部通过（含 FSRS 算法行为验真测试），Maven 编译 100% 成功；
  * 前端 `npx tsc --noEmit` 0 报错，无类型失配。

### 🟢 记录点 #29：闪卡零词无限加载转圈堵塞排查与彻底根治：清除所有静态Mock假词、重构双模空状态看板与水合看门狗防护
* **时间**：2026-09-13
* **执行角色**：全栈排障架构师 & 认知引擎工程师
* **问题现象与排查分析（Root Cause Analysis）**：
  * **用户反馈**：
    1. “优化闪卡复习，如果复习和学习没有单词的话，不要一直显示图片中的，可能是堵塞了，你先排查原因，然后告诉我，我记得他们有单词是有相应的页面显示的”；
    2. “删除前端静态保存的一些单词数据，不然会导致巩固复习页面闪卡清空后静态数据会显示出来”；
  * **无限转圈（"正在拉取复习卡片队列..."）深度排查根因**：
    1. **SSR 水合失配中断（Hydration Mismatch Stall）**：`cards/page.tsx` 中 `learnBatchSize` 状态在 `useState` 初始声明时直接调用了 `localStorage.getItem`，导致服务端渲染（SSR）默认值为 10，客户端水合时为缓存值，引发 React 19 / Turbopack 内部水合不一致警告并挂起客户端首次挂载流；
    2. **底层 Fetch 缺失超时保护**：前端 `api-client.ts` 的原生 `fetch` 未配置 `AbortController`，在遇到代理连接重置或网络抖动时 Promise 处于未决状态，导致 `Promise.all` 无法返回，`loading` 始终卡在 `true`；
    3. **空状态路由混淆**：复习模式此前未区分“本轮刚复习完（有完成记录）”与“队列本就为空或已清空”，在零词时误展示了需要从复习记录开启默写的通关结算板；
  * **静态假数据回潮根因**：
    1. **前端 Mock 数据残留**：`src/data/mock-lexiflow.ts` 中原本写死了 `provoke`、`precarious` 等 5 个静态单词与若干例句，部分看板组件在无数据时直接回退读取了该静态数组；
    2. **后端自动字典注入**：`ReviewServiceImpl.getNewWordsQueue` 原本存在 `if (totalUserCards == 0) { 自动从词典拉取10个词注入 }` 的兜底代码，导致用户清空后一刷新，底层又自动被填充了 10 个生词。
* **本阶段做了什么（What Was Done）**：
  1. **清除全栈所有静态 Mock 假数据与自动强行注入**：
     - **后端**：在 `ReviewServiceImpl.java` 中彻底删除了零词时自动强行从 `dict_entry` 抽取 10 个词的注入逻辑；优化 `clearReviewQueue` 确保 100% 物理清空已学与待复习卡片；
     - **前端**：彻底清空 `mock-lexiflow.ts` 中的 `mockVocabList = []` 与 `mockContextCaptures = []`；同步清理影子跟读预设中的假生词；为全局精选短句组件 (`daily-curated-quote.tsx`) 和语境快照组件 (`recent-captures.tsx`) 增加优雅的零数据空状态占位，杜绝静态假词回潮。
  2. **根除无限 Loading，建立全链路双重看门狗超时机制**：
     - **API 客户端看门狗**：在 `api-client.ts` 的 `request()` 中装配 8000ms 的原生 `AbortController` 超时控制器，网络异常或挂起时主动抛出异常而非永久等待；
     - **页面级 4s 强制看门狗**：在 `cards/page.tsx` 的 `loadAllQueues` 中增加 4000ms `Promise.race` 看门狗机制，不论底层网络何等异常，`finally { setLoading(false) }` 100% 准时执行，永不卡在转圈态；
     - **修复 SSR 水合安全**：将 `learnBatchSize` 声明改为安全静态默认值，通过客户端专属的 `useEffect` 挂载后安全读取 `localStorage`。
  3. **重构「巩固复习」与「研习新词」双模式专属空状态看板**：
     - **复习队列为空专用看板**：当 `reviewQueue.length === 0 && completedReviewCards.length === 0` 时，展示纯净优雅的空状态界面（绿色勾选徽标、`今日暂无待巩固复习的单词闪卡`、直达「前往研习新词」、「去词书库挑选」、「刷新检查」三联快捷按钮）；
     - **结算看板保留**：仅当用户在本轮会话中真实完成过复习卡片打卡（`completedReviewCards.length > 0`）后，才触发通关挑战与单词默写看板。
  4. **全栈构建与端口验证**：
     - 前端 `pnpm tsc --noEmit` 0 报错；
     - 后端 `mvn test-compile` 100% 通过（`BUILD SUCCESS`）；
     - 端口探测：Next.js (3000)、Spring Boot (8080) 运行正常，`/api/review/queue` 与 `/api/review/new-queue` 清空后准确响应纯净空数组 `[]`。

---

### 🟢 记录点 #28：清空闪卡功能全栈落地：复习队列与未学新词队列严格解耦隔离，双模独立治理
* **时间**：2026-09-13
* **执行角色**：全栈架构师 & 认知算法工程师
* **需求背景与痛点根因**：
  * **用户需求**：“新增一个清空闪卡的按键，复习和学习新词是两个分开的逻辑”；
  * **核心痛点与逻辑混淆**：
    1. 闪卡页面包含两种完全不同的认知学习模式——「巩固复习 (REVIEW)」与「研习新词 (LEARN)」；
    2. 旧有复习队列拉取接口中混入了 `state == 0` 的新词，且在新词队列不足时会自动从字典库填充 10 个词，导致用户若尝试清空新词会被强制重新注入，造成“怎么清都清不完”的困境；
    3. 复习队列包含处于学习中 (`state=1`)、复习中 (`state=2`)、再学习 (`state=3`) 的记忆卡片，属于认知加固流；而新词队列属于纯粹未学过的新生词 (`state=0`)。两者的生命周期、重置边界与认知意义截然不同，必须从底层数据库、后端控制器、前端 API 客户端到 UI 交互全面实行**100% 物理与逻辑严格隔离**。
* **本阶段做了什么（What Was Done）**：
  1. **后端 Review 模块双通道清空与队列隔离重构**：
     - **接口定义扩展**（`ReviewService.java`）：新增 `int clearReviewQueue(Long userId)` 与 `int clearNewWordsQueue(Long userId)`；
     - **业务实现隔离**（`ReviewServiceImpl.java`）：
       * `clearReviewQueue`：仅删除当前用户 `isKnown = 0 && state != 0` 的记录，彻底清空所有在学/复习中的旧卡片，**绝不触碰**任何未学新词（`state = 0`）；
       * `clearNewWordsQueue`：仅删除当前用户 `state = 0` 的未学新词记录，**绝不触碰**任何复习流中的卡片或历史复习日志；
       * **修复新词自动注入逻辑**：将 `getNewWordsQueue` 的自动从 `dict_entry` 抽取新词限制为仅当用户为零记录新用户（`totalUserCards == 0`）时才执行，用户主动清空后不会发生自动强行注入；
       * **彻底分离复习查询逻辑**：`getReviewQueue` 去除 `.or().eq(state, 0)`，严格只查询已学习且到期的复习卡片；
     - **RESTful 端点暴露**（`ReviewController.java`）：
       * `POST /api/review/queue/clear` $\to$ 清空复习队列；
       * `POST /api/review/new-queue/clear` $\to$ 清空未学新词队列；
  2. **前端 API Client 严谨对接**（`api-client.ts`）：
     - 在 `reviewApi` 对象中注册 `clearReviewQueue()` 与 `clearNewWordsQueue()` 强类型方法；
  3. **前端工作台清空交互与模式感知**（`cards/page.tsx`）：
     - **顶栏右侧清空按钮**：在模式栏右侧醒目添加 `[清空闪卡]` 按钮（配备 `Trash2Icon`，自适应玫瑰红危险态与悬浮放大微动效）；
     - **语义化防误触二次确认弹窗（Modal）**：
       * 动态感知当前激活的模式（`activeMode === "REVIEW"` vs `"LEARN"`）；
       * 明确显示即将清空的卡片数量（如当前待复习或当前待研习新词数量）；
       * 详细标注两种模式的隔离规则文案，打消用户对“误删学习历史”或“误清新词库”的疑虑；
       * 异步提交中展示转圈等待态，操作完成后通过顶部全局悬浮灵动胶囊即时播报清空结果并自动刷新视图。
  4. **全链路验证与编译**：
     - 后端 `mvn test-compile` 100% 通过（`BUILD SUCCESS`）；
     - 前端 `pnpm tsc --noEmit` 0 报错；
     - 前端 `curl http://localhost:3000/cards` 返回 HTTP 200 OK。

---

### 🟢 记录点 #27：研习新词（LEARN）交互形态重构：四选一与全解原位无缝置换，彻底杜绝页面纵向溢出与滚动条
* **时间**：2026-09-13
* **执行角色**：前端交互架构师 & 认知体验专家
* **优化背景与痛点根因**：
  * **用户反馈**：“学习新单词的时候也会出现这种问题页面会变得可滚动。”，并上传笔记本真机截屏；
  * **根因剖析**：此前在「研习新词」模式中，选完选项或点击「不认识 / 看详解」后，原有的 4 个选项按钮依然常驻 DOM（占据 ~120px），并在其下方追加展开词性、释义与语境真题例句大框（占据 ~200px），导致大卡片高度从 380px 暴增至 700px+，叠加顶部导航与工具栏后整体高度超出笔记本屏幕安全视口（720px~800px），产生纵向滚动条，并将底部的“下一个新词 [Enter]”核心操作按钮挤出屏幕外，破坏高频刷词的心流；
  * **设计原则**：对于闪卡类高频认知工具，卡片无论处于哪种状态，界面**必须严格 100% 贴合单屏视口（Zero Viewport Overflow）**，确保手指与视线零额外滚动消耗。
* **本阶段做了什么（What Was Done）**：
  1. **核心交互区原位置换（In-Place State Swap，零高度膨胀）**：
     - 未答题时：中部槽位展示四选一选项按钮（A/B/C/D）；
     - 已答题或看全解时：在**完全相同的中部槽位**原位淡入“释义反馈横幅 + 语境真题例句卡”，优雅隐退已无交互价值的选项按钮组；
     - 答题前后高度差由原先的 +320px 暴降至 $\le$ 30px，卡片总高度严控在 ~350px-370px；
  2. **高阶释义与对比横幅设计**：
     - 自适应三种认知状态语义色：
       * 🟢 辨析正确：翡翠绿（Emerald）+ `[✓ 辨析正确]` + `命中选项 [A]`；
       * 🔴 选错偏差：玫瑰红（Rose）+ `[✗ 需重点加固]` + `你选了 [B] · 正确为 [A]`；
       * 🟡 主动求助：暖琥珀（Amber）+ `[💡 新词认知]` + `正确选项: [A]`；
     - 词性标签 + 中文释义 + 英文释义精准呈现；
     - 语境真题例句卡整合标准发音朗读与快捷键 `[S]`；
  3. **闪卡双模式视口高度紧凑化工程（Laptop Viewport Hardening）**：
     - 页面外层间距优化：`gap-6` $\to$ `gap-4 sm:gap-5`；
     - 闪卡大卡片内边距优化：`p-8 sm:p-10` $\to$ `p-5 sm:p-7`，单此一项即节省 40px 纵向高度；
     - 单词标题字号精细标定：`text-4xl sm:text-5xl md:text-6xl` $\to$ `text-3xl sm:text-4xl md:text-5xl`，兼顾辨识度与屏效；
     - 复习模式（REVIEW）卡片同步紧凑化，确保无论翻转前后、无论何种屏幕分辨率，全站**100% 锁死单屏内，0 纵向滚动条，Enter 提交键全时可见**；
  4. **工程质量与构建验证**：
     - `pnpm build` 25 个路由 100% 编译通过，Turbopack 10.3s，TypeScript 0 报错；
     - 本地 3000 端口与 Nginx 96 端口双向健康探测均返回 HTTP 200 OK。

---

### 🟢 记录点 #26：全站反馈体系规范化：击键默写、阶段通关与影子跟读全面脱流防抖
* **时间**：2026-09-13
* **执行角色**：系统架构师 & 前端体验工程师
* **优化背景与深度思考**：
  * 用户提出：“同样的击键默写也有一样的问题”；
  * 排查发现，此前在击键默写（`KeystrokeCardPanel`）提交判定、通关挑战（`challengeSession`）以及影子跟读工坊（`/practice/shadowing`）中，触发的即时反馈信息同样挂载在内部容器的正常文档流（In-Flow）中，导致键盘盲打输入框与音频控制栏随反馈出现/消失发生 ~60px 的上下位移；
  * **键盘盲打的核心痛点**：对于盲打用户，视线和手指正高度聚焦于固定的槽位和输入框，任何非预期的位移都会破坏节奏。
* **本阶段做了什么（What Was Done）**：
  1. **闪卡全模式（翻转/击键/新词/考核）反馈统一**：
     - 击键客观判定（`⚡ 客观动力学已纳管`）、通关考核（`⚡ 通关客观已判定`）、放弃记遗忘（`↺ 已记为遗忘`）全部统一接驳全局脱流的顶部悬浮灵动胶囊；
     - 击键输入卡片与实时 4 列动力学 HUD（首键潜伏期、犹豫摩擦力、退格试错、提示求助）**垂直坐标绝对锁定，0 抖动**；
  2. **影子跟读工坊同步彻底脱流**：
     - 将 `/practice/shadowing` 页面原本位于题源切换与标准句卡片之间的 `feedbackToast` 彻底拔除；
     - 移植为 `fixed top-5 left-1/2 -translate-x-1/2 z-50` 悬浮灵动胶囊，录音评测与分句切换时录音区与波形图绝对稳定；
  3. **质量与构建闭环**：
     - `pnpm build` 全站 25 个静态与动态路由 100% 编译通过，无任何 TypeScript 报错。

---

### 🟢 记录点 #25：操作反馈形态重构：拔除文档流挤压横幅，升级为零布局抖动（Zero-CLS）悬浮灵动胶囊
* **时间**：2026-09-13
* **执行角色**：前端主程 & 交互动效专家
* **优化背景与痛点根除**：
  * **原痛点**：用户点击底部四级评分（遗忘/困难/良好/简单）时，原反馈提示框直接插在顶栏工具条与卡片之间（In-Flow DOM），每次弹出都会向下强行推挤卡片 ~60px，3 秒消失后卡片又向上跳回，造成严重的累计布局偏移（CLS）与视觉不适；
  * **形态选型分析**：若采用居中强阻塞弹窗（Modal Dialog），会导致用户每评一张卡片都必须额外点击一次关闭，严重打断闪卡复习的高频心流；
  * **最佳实践方案**：采用类似 Apple 灵动岛 / Raycast HUD 的**顶部居中悬浮毛玻璃胶囊（Floating Glass HUD Toast）**。
* **本阶段做了什么（What Was Done）**：
  1. **完全脱离文档流（Zero-CLS）**：
     * 从页面垂直容器流中彻底拔除原有的静态横幅，卡片与下方按钮垂直位置绝对锁定，点击评分卡片**0 像素抖动、0 挤压**；
  2. **悬浮灵动胶囊架构落地**：
     * 采用 `fixed top-5 left-1/2 -translate-x-1/2 z-50` 优雅悬浮于顶部，微带 `backdrop-blur-md` 毛玻璃与深浅模式自适应底色；
     * 丝滑滑入动效：`animate-in fade-in slide-in-from-top-3 duration-200`，支持右上角微型 `✕` 即时手动关闭；
  3. **多模态自适应语义色彩与徽标**：
     * 遗忘 / Again $\to$ 玫瑰红（Rose）+ `RotateCcwIcon`；
     * 困难 / Hard $\to$ 暖琥珀（Amber）+ `ClockIcon`；
     * 良好 / Good $\to$ 天空蓝（Sky）+ `CheckCircle2Icon`；
     * 简单 / Easy $\to$ 翡翠绿（Emerald）+ `CheckCircle2Icon`；
     * 击键动力学 $\to$ 极光紫（Violet）+ `ZapIcon`；
  4. **全量构建验证**：
     * `pnpm build` 25 个静态与动态路由 100% 编译通过，TypeScript 0 错误。

---

### 🟢 记录点 #24：通关看板闭环锁定与可自定义研习词量配置中心落地
* **时间**：2026-09-13
* **执行角色**：系统架构师 & 全栈工程师
* **需求背景与认知科学机制**：
  * 响应用户指令：“每组单词学完之后要保留剩下的两个模块，然后在闪卡界面增加一个每次学习单词数量的设定，可以自定义每组单词的数量”；
  * **通关状态锁定**：此前每组学完最后一个词后直接重拉全量队列并重置索引为 0，导致跳过结算看板直接进入下一组。现将每组完成态明确纳管（`isLearnGroupCompleted = true`），确保每组学完必出双模块看板；
  * **容量控制（米勒定律）**：短期工作记忆广度为 $7 \pm 2$ 项，支持动态自适应调节每组新词容量（3~100 词），避免认知超载。
* **本阶段做了什么（What Was Done）**：
  1. **每组完成态生命周期锁定（保留两大核心模块）**：
     * `handleNextNewWord` 与 `handleKillWord` 到达本组末尾时，立即触发 `isLearnGroupCompleted = true`，呈现结题看板；
     * 强力保留两大通关模块：
       - 🌟 **模块 1（极速锁固）**：开启新词击键通关（盲打刚才这一组的词汇，由击键动力学测定初始 $S_0, D_0$）；
       - 🔄 **模块 2（复习流）**：切换至巩固复习（平滑过渡至今日到期卡片）；
     * 附带「直接研习下一组」与「调整每组词量」次级快捷操作。
  2. **闪卡界面全栈接入自定义词量配置**：
     * **持久化状态**：初始化从 `localStorage.getItem("lexiflow_learn_batch_size")` 读取配置（默认 10 词/组）；
     * **顶栏快捷入口**：分段切换器右侧常驻 `[⚙️ 每组: 10 词]` 配置徽标，点击即时呼出配置面板；
     * **学习卡片顶部实时入口**：背词卡片顶部进度指示处增加 `[⚙️ 10词/组]` 快速微调胶囊；
     * **高阶毛玻璃配置弹窗（Modal）**：
       - 预设四档高频推荐（5 词、10 词 🌟、15 词、20 词、25 词、30 词）；
       - 自定义数值输入框（支持 3 ~ 100 任意词量，回车/按钮秒级生效）；
       - 认知心理学小贴士（阐述工作记忆广度与学后即测转化机制）；
     * **后端服务对齐**：更新 `ReviewServiceImpl.java` 最大拉取上限至 100 词，满足各种强度的刷词需求。
  3. **质量与构建验证**：
     * 修正事件监听器类型；
     * 执行 `pnpm build`，全站 25 个路由 100% 构建通过，TypeScript 0 错误；
     * 端点探测：`GET /cards` 返回 HTTP 200。

---

### 🟢 记录点 #23：新词通关看板视觉流聚焦：移除多余词书库导流卡片，强化闭环记忆链路
* **时间**：2026-09-13
* **执行角色**：全栈工程师 & 交互体验设计师
* **优化背景与深度思考**：
  * 用户在新词学习通关后，当前首要认知诉求是「即时锁定刚学新词的记忆表征（击键通关）」或「无缝衔接进入今日复习队列」；
  * 原先并列展示的「挑选更多大纲词书」属于宽泛的信息架构导流分支，与当下的“吸收与强化闭环”心流冲突，造成选择过载。
* **本阶段做了什么（What Was Done）**：
  1. **移除导流冗余**：彻底删除研习新词阶段结算看板中的「挑选更多大纲词书」外链卡片；
  2. **网格视觉平衡调优**：将通关选项网格自适应调整为居中平衡的双列卡片（`grid-cols-1 sm:grid-cols-2 max-w-xl`）：
     - 🌟 **选项 1（极速锁固）**：开启新词击键通关（盲打刚学新词，由动力学算法确定 $S_0, D_0$）；
     - 🔄 **选项 2（复习流）**：切换至巩固复习（无缝流转，解决今日到期队列）；
  3. **清理未使用的图标依赖**：移除 `BookOpenIcon` 引用，保障代码干净无警告；
  4. **全量构建验证**：`pnpm build` 25 个路由 100% 编译通过，无任何 TypeScript 报错。

---

### 🟢 记录点 #22：信息架构认知重构：击键默写降维下沉，落地「学-测分离」阶段通关看板
* **时间**：2026-09-13
* **执行角色**：产品体验专家 & 全栈架构师
* **优化背景与深度思考**：
  * 针对用户提出的核心洞察：“敲击默写不应该和研习新词、巩固复习同级，它应该在某个阶段之后”进行彻底整改；
  * 理清信息架构概念层级错位（Category Mistake）：“研习新词”与“巩固复习”是记忆生命周期的业务阶段，而“击键默写”是产出型考核手段；一上来直接盲打会导致极高的认知阻抗。
* **本阶段做了什么（What Was Done）**：
  1. **顶栏减负重构（恢复纯粹的生命周期双模）**：
     * 移除顶栏分段控制器中的同级 `[击键默写]` 按钮，仅保留 `[🔄 巩固复习]` 与 `[✨ 研习新词]` 两大纯粹入口；
     * 清晰划分复习队列与新词池的计数显示。
  2. **落地两阶段认知漏斗（Study-then-Test Effect）通关可选项看板**：
     * **场景 A：【巩固复习】初轮完成看板**：
       - 当今日到期卡片全部翻完后，不再只是简单的“队列已清空”，而是弹出**结算与进阶通关选项看板**；
       - 🌟 **选项 1（强烈推荐）：开启「击键动力学通关考核」**：无提示盲打刚才复习过的这一批单词，由毫秒级潜伏期 $RT$ 与退格率客观裁决并刷新 FSRS 记忆稳定性；
       - 🎙️ **选项 2：前往「影子跟读工坊」**：对今日重点句进行原声语流跟读与 DP 对齐纠错；
       - ✨ **选项 3：切换至「研习新词」模式**：无缝衔接扩充词库。
     * **场景 B：【研习新词】一组完成看板**：
       - 当完成本组新词的四选一辨析输入后，弹出**新词吸收结算看板**；
       - 🌟 **选项 1（极速锁固）：开启「新词击键通关考核」**：将刚学的新词进行盲测拼写，由动力学算法量身生成最精准的初始参数 $S_0$ 与 $D_0$；
       - 🔄 **选项 2：切换至巩固复习**；
       - 📚 **选项 3：去大纲词书库挑选更多词汇**。
  3. **复习过程中保留即时微调开关**：
     * 在复习卡片右上角加入轻量胶囊开关 `[ 🎴 3D翻转 | ⌨️ 击键默写 ]`，重度键盘盲打用户无需等待整轮结束，在复习过程中可随时切换为盲打默写卡片，充分保障用户自主权。
  4. **构建与稳定性验证**：
     * 重新构建 Next.js 生产包，25 个页面路由编译通过，TypeScript 0 错误。

---

### 🟢 记录点 #21：【核心得分武器】击键动力学闪卡深度内嵌 × 影子跟读词级 DP 强制对齐工坊落地
* **时间**：2026-09-12
* **执行角色**：算法工程师 & 全栈架构师
* **战略对齐与纠偏背景**：
  * 严格对齐《算法大赛参赛指南》第四节“核心贴赛亮点与技术包装（得分武器库）”；
  * 彻底叫停非核心应用 CRUD，聚焦自研核心算法与多模态视听亮点，冲击大赛满分项（25分实现完成度 + 20分创新设计 + 20分AI开源融合）。
* **本阶段做了什么（What Was Done）**：
  1. **武器一：闪卡模块（`/cards`）全功能内嵌「击键动力学客观行为映射（Keystroke Dynamics）」**：
     * **架构解耦与模式升级**：在闪卡复习顶栏增加 `[巩固复习 (3D翻转)]`、`[击键默写 (⚡AI客观映射)]`、`[研习新词]` 三模分段切换，彻底打破打字默写与记忆调度的传统割裂；
     * **毫秒级物理键盘动力学采样器**：新建 `KeystrokeCardPanel.tsx`，纳秒级捕获键盘事件：
       - 首键提取潜伏期 $RT = t_{\text{first\_key}} - t_{\text{start}}$（反映大脑词汇正字法激活延迟）；
       - 键间间隔序列 $IKD_i = t_i - t_{i-1}$ 与标准差 $IKD_{\sigma}$（客观衡量拼写犹豫度与认知摩擦力）；
       - 退格修正次数 $Backspaces$（记录运动试错损耗）；
       - 提示求助惩罚 $Hints$（`[Tab]` 揭示首字母脚手架惩罚）；
     * **自研客观行为评价函数** $\Phi(Acc, RT, IKD_{\sigma}, Backspaces, Hints) \to \text{Rating} \in \{1, 2, 3, 4\}$：
       - 拼写有误 $\to$ `Again (1)`；
       - 依赖提示或 $Backspaces > 2$ 或 $RT > 3500\text{ms}$ $\to$ `Hard (2)`；
       - 极速瞬析盲打（$RT < 1200\text{ms}$ 且 0 退格且键间平稳） $\to$ `Easy (4)`；
       - 正常流利拼写 $\to$ `Good (3)`；
     * **Zero-Click 自动推进与 HUD 平视显示器**：实时展示潜伏期、犹豫指数、退格数；输入正确瞬间触发动力学徽章并自动提交 `reviewApi.submitRating`，驱动 FSRS-4.5 状态机流转，彻底消灭用户自评的主观噪声！
  2. **武器二：自研纯前端词级动态规划强制对齐算法引擎（`word-aligner.ts`）**：
     * 建立 $(M+1) \times (N+1)$ 二维代价转移矩阵，支持字符级 Levenshtein 编辑容差与清浊音偏差判定；
     * 运行最优路径回溯（Traceback），精准识别基准文本每个单词的细粒度发音状态：
       - 🟢 **`CORRECT`**（发音吻合）
       - 🔴 **`SUBSTITUTION`**（发音偏差，附带用户实际读出的词与音标浮标）
       - 🟡 **`OMISSION`**（吞音或漏读，以灰色虚线划线标注）
       - ⚪ **`INSERTION`**（多读杂音，以黄色徽标提示）
     * 输出四维口语量化模型：准确度 (Accuracy)、完整度 (Completeness)、流利度 (Fluency)、综合总分 (Overall Score)。
  3. **武器二：影子跟读智能评测工坊（`/practice/shadowing`）全栈交互落地**：
     * **Canvas 声波示波器（`audio-waveform.tsx`）**：基于 Web Audio API（`AudioContext` + `AnalyserNode`）实时绘制绿色动态柱状声波振幅动画；
     * **`ShadowingDiff` 词块胶囊组件（`shadowing-diff.tsx`）**：严格按照设计规范实现四态胶囊高亮渲染，支持鼠标悬浮查看音标对比，支持点击任意单词单点朗读标准原声；
     * **题源深度联动**：支持切换 BBC 真实外刊重点句（联动刚才抓取的 145+ 篇 BBC 文章）、生词本原生例句、以及用户自定义输入例句；
     * **听觉对照闭环**：提供标准原声 [A] vs 用户录音 [B] 的 AB 试听对比条，支持 1.0x / 0.8x 慢速口型跟读；
     * **预留后端标准接口**：封装清晰的转录与评测契约，方便后续接入后端高精度 Whisper 容器。
  4. **全站导航与路由集成**：
     * 在 `app-sidebar.tsx` 中新增 **「AI 算法工坊」** 导航分组，加入 **「影子跟读」** 直达入口。
* **质量与构建验证**：
  * 执行 `pnpm build`，全站 **25 个静态与动态路由 100% 编译通过**，TypeScript 0 错误，打包耗时 7.5s，代码零警告。

---

### 🟢 记录点 #20：深度外刊阅读库（`/reading`）与 BBC 官方实时新闻流抓取全栈联调
* **时间**：2026-09-12
* **执行角色**：数据爬虫工程师 & 全栈架构师
* **本阶段做了什么（What Was Done）**：
  1. **数据模型与依赖建立**：
     * 新建 MySQL `reading_article` 文章库表（包含频道、来源、标题、原文链接、GUID 唯一标识、封面图、导读、纯净段落 JSON、字数、CEFR 评级、核心生词）；
     * 后端引入 `org.jsoup:jsoup:1.17.2` 解析库，支持 RSS XML 解析与 HTML 网页清洗。
  2. **BBC 官方 5 大核心频道 RSS 实时流打通**：
     * 覆盖国际时事 (`WORLD`)、科技前沿 (`TECH`)、商业财经 (`BUSINESS`)、科学环境 (`SCIENCE`)、文化娱乐 (`ENTERTAINMENT`)；
     * 实现自适应直连/本地代理（7897）容灾探测，秒级完成新闻元数据增量抓取与 GUID 去重持久化，实测成功入库 **145+ 篇 BBC 真实报道**。
  3. **智能语言学分析与懒加载全文本提取**：
     * 基于全文字频、平均句长与词长分布，自研算法推算 **CEFR 语言难度定级（B1 / B2 / C1 / C2）** 并预估阅读耗时；
     * 智能提取文章中的 3~5 个高阶重点考纲词条；
     * 实施懒加载机制：RSS 极速入库导读，用户点击研读卡片时，即时通过 Jsoup 提取纯净正文段落（单篇清洗提取出 61 个高质量段落）并持久化缓存。
  4. **杂志货架 (Bento Grid) 与沉浸式 ZenReader 研读器**：
     * 杂志货架卡片流：高清封面、频道徽标、彩色 CEFR 徽章、相对发布时间与核心生词标签；
     * 沉浸式纸感研读抽屉：支持衬线/黑体切换、A-/A/A+ 三档字号缩放、纸白/羊皮纸/暗夜三色纸质主题；
  5. **行间划词查词与一键语境采词闭环（核心黑科技）**：
     * 阅读正文中点击任意英文单词，底部即时弹出浮动释义卡片，展示音标、词性、精准柯林斯中文释义与原生朗读；
     * 点击「一键入库生词本」，自动将**“目标单词 + 当前句子原文例句 + BBC 语料来源”**直接录入 `user_word` 表（`source = 'READING'`），毫秒级推入 FSRS-4.5 复习流，正文单词自动变绿高亮标记。
* **质量与构建验证**：
  * 后端接口验证通过，`GET /api/reading/channels` 与 `GET /api/reading/articles/1` 返回 200；
  * 前端 `pnpm build` 全站 24 个静态与动态路由 100% 编译通过，TypeScript 0 错误。

---

### 🟢 记录点 #19：原生语境生词本（`/vocab`）真实后端对接与 FSRS 记忆指标全透视
* **时间**：2026-09-12
* **执行角色**：后端主程 & 前端全栈工程师
* **本阶段做了什么（What Was Done）**：
  1. **彻底移除前端静态 Mock**：
     * 全面废弃旧版 `mockVocabList`，完全接入 Spring Boot `VocabularyController` 与 MySQL 真实库表。
  2. **多维动态指标看板**：
     * 顶部指标看板由 `GET /api/vocab/overview` 动态驱动：实时展示词库总容量（62词）、今日待复习（57词）、新词池（15词）、长效巩固（43词）、已掌握/斩词（4词）。
  3. **五大认知阶段筛选与服务端真分页**：
     * 全部词条、今日到期（`isDue=true`）、新词待学（`state=0`）、巩固复习（`state=-1`）、已掌握（`isKnown=1`）；
     * 后端扩展 `keyword` 与 `isDue` 联合查询，支持原形、中文释义与例句模糊搜索；
     * 客户端支持 10 / 20 / 50 条分页与页码切换。
  4. **卡片即时交互与 FSRS 算法透明化呈现**：
     * 原生美音音频试听与 Web Speech API 容灾朗读；
     * 标熟斩词/移回复习一键切换，调用 `POST /api/vocab/cards/{id}/mark-known` 实时生效；
     * 安全删除确认弹窗，调用 `DELETE /api/vocab/cards/{id}`；
     * 全景展示每张卡片的稳定性（$S$）、认知难度（$D$）、预测留存率（$R$）、相对下次复习时间与历史复习轮次；
     * 一键导出 18 维全字段 UTF-8 BOM CSV 档案；订阅 `lexiflow_wordbook_updated` 全局无感同步。

---

### 🟢 记录点 #18：今日概览（`/dashboard`）与当前主词书全景动态联动
* **时间**：2026-09-12
* **执行角色**：前端主程
* **本阶段做了什么（What Was Done）**：
  1. **告别静态 Mock**：
     * `<HeroMissionCard />` 与 `<DailyCuratedQuote />` 彻底移除硬编码数据，动态读取当前主词书 ID；
     * 并行请求 `GET /api/wordbooks/{id}` 与 `GET /api/wordbooks/{id}/status-counts`，实时呈现主词书标题、总词量、已标熟词数及标熟掌握度。
  2. **任务与进度动态推演模型**：
     * 到期复习量（`dueCount`）精准反映今日复习负荷；
     * 预计耗时动态预估：`Math.max(3, Math.ceil(dueReview * 0.6 + min(newWords, dailyTarget) * 0.9))` 分钟；
     * 通关日期预测：根据未标熟剩余量与每日研习目标动态推算；
     * 每日研习量微调：支持选择 10~50 词，即时本地持久化并重算指标。
  3. **风格与体验校准**：
     * 严格遵从冷灰高阶设计原则，去除霓虹绿色 logo 与多余时间戳，视觉回归克制统一。

---

### 🟢 记录点 #17：真实词库标熟掌握度黄金标准计算与全链路响应式联动
* **时间**：2026-09-12
* **执行角色**：算法与业务逻辑工程师
* **本阶段做了什么（What Was Done）**：
  1. **确立“标熟/斩词”为掌握度唯一黄金标准**：
     * 纠偏传统背单词产品将“加入复习库”作为进度的弊端，将进度条严格定义为“实际标熟建立长期记忆的比例”；
     * 进度百分比严格按 `(masteredWords / totalWords) * 100.0` 计算；
     * 标熟操作立即推进进度，移回在学自动平滑回退；
  2. **跨模块实时解耦同步**：
     * 建立 `lexiflow_wordbook_updated` 全局广播与 window `focus` / `storage` 监听机制，多标签页与多组件间零手动刷新自愈同步。

---

### 🟢 记录点 #16：词书安全删除与智能双层动态排序引擎
* **时间**：2026-09-12
* **执行角色**：后端架构师 & 前端算法工程师
* **本阶段做了什么（What Was Done）**：
  1. **级联安全删除**：
     * 调用 `DELETE /api/wordbooks/{id}` 时，后端级联清理 `wordbook_item` 关联条目，并将 `user_word` 中的引用安全解绑，**完整保留用户 FSRS 记忆周期与复习成果**；
     * 前端配合毛玻璃警告对话框，防止误删。
  2. **双层动态排序算法**：
     * 第一优先级：当前主研习词书（`primaryId`）始终锁定排在第一位（TOP 1）；
     * 第二优先级：其余词书根据用户的实际点击/访问历史事件（Click History Events）倒序排列；
     * 点击事件与主词书设定持久化至 `localStorage`。

---

### 🟢 记录点 #15：通用词书导入器与智能 JSON 词库递归清洗引擎
* **时间**：2026-09-12
* **执行角色**：NLP 与数据清洗工程师
* **本阶段做了什么（What Was Done）**：
  1. **跨格式导入支持**：
     * 支持 `.csv`, `.txt`, `.tsv`, `.json` 格式上传解析；
  2. **智能 JSON 递归解析与清洗引擎**：
     * `cleanAndValidateLemma` 算法拦截包含代码符号（`[`、`{`、`:`、`"` 等）、数据库字段（`null`、`status` 等）与非英文字符；
     * 递归检索多层嵌套 JSON（百词斩、扇贝、欧路、有道等）；
     * `UTF-8` + `GB18030/GBK` 自适应流解码，彻底杜绝 Windows 编码乱码与截断。

---

### 🟢 记录点 #14：词书研习工作台 5 阶分类侧边栏、小眼睛盲测与时间轴分组
* **时间**：2026-09-12
* **执行角色**：前端主程 & 交互设计师
* **本阶段做了什么（What Was Done）**：
  1. **5 阶分类侧边栏**：全部 (`ALL`)、未学习 (`UNLEARNED`)、复习中 (`REVIEWING`)、复习完成 (`COMPLETED`)、已标熟斩词 (`MASTERED`)，实时动态角标；
  2. **小眼睛主动回忆隐蔽模式 (Active Recall)**：默认隐藏音标与中文释义，仅保留纯英单词，支持点击即时窥探与折叠；
  3. **已标熟按日期分组与快速筛选**：时间轴动态聚类展示历史斩词成果；
  4. **抽屉式沉浸浮层**：URL Query 参数驱动（`?bookId=xxx`），结合 `createPortal` 与滚动锁定。

---

### 🟢 记录点 #13：构建后端架构、MySQL 8 库表、FSRS-4.5 调度引擎与 OpenAPI 3 标准文档
* **时间**：2026-09-12
* **执行角色**：系统架构师 & 后端主程 & 记忆算法工程师
* **本阶段做了什么（What Was Done）**：
  1. **响应用户需求与设计落地**：
     * 响应用户指令：“先把后端框架搭好再来实现好一点，密码是 root，在你写接口的时候要正确使用 @Api 等接口方便我后期生成接口文档”；
     * 选型构建现代企业级后端：Spring Boot 3.2.3 + Java 17 + MySQL 8.0 + MyBatis-Plus 3.5.5 + JJWT 0.12.5 + SpringDoc OpenAPI 3.0 + Spring Security 6。
  2. **MySQL 8.0 物理建模与初始数据就绪（`lexiflow_db`）**：
     * 执行 DDL 规范建表：`sys_user`（用户表）、`dict_entry`（双语词典表）、`wordbook`（大纲词书表）、`wordbook_item`（词书条目表）、`user_word`（用户生词卡片表）、`review_log`（复习打卡日志流水表）、`daily_stat`（每日学习打卡聚合表）；
     * 预置种子数据：测试用户（`lin` / `123456`）、3 本大纲词书（CET-4、CET-6、日常高频口语）、30 条精选词典条目、12 张多样状态的 FSRS 用户词卡、以及 30 天的每日研习打卡历史数据。
  3. **FSRS-4.5 自由间隔复习调度核心算法实现（`com.lexiflow.modules.review.fsrs`）**：
     * 严谨复刻最新 FSRS-4.5 数学模型（17 参数权重向量）：
       * 记忆保留度预测：$R(t, S) = (1 + \text{FACTOR} \cdot t / S)^{-0.5}$；
       * 初始稳定性与初始阻抗：$S_0(G) = w_{G-1}$，$D_0(G) = w_4 - e^{w_5(G-1)} + 1$；
       * 阻抗均值回归：$D'(D, G) = w_7 \cdot D_0(3) + (1 - w_7) \cdot (D - w_6(G - 3))$；
       * 成功复习稳定性跃迁：$S'_r(D, S, R, G) = S \cdot \left(1 + e^{w_8}(11-D)S^{-w_9}(e^{w_{10}(1-R)}-1)h(G)\right)$；
       * 遗忘重学稳定性惩罚：$S'_f(D, S, R) = w_{11} D^{-w_{12}} ((S+1)^{w_{13}}-1) e^{w_{14}(1-R)}$；
       * 四档预估间隔按钮生成：针对 1(Again)、2(Hard)、3(Good)、4(Easy) 实时推算人类可读时间间隔（如 `10m`、`1d`、`3d`、`8d`），直供前端闪卡界面按钮。
  4. **全量接入 OpenAPI 3 / Swagger 标准文档注解**：
     * 全面遵循 Spring Boot 3 与 OpenAPI 3 规范（`@Tag`, `@Operation`, `@Schema`, `@Parameter`, `@ApiResponse`），详细描述每个接口的中文业务意图、字段约束、参数示例及返回格式；
     * 涵盖 6 大核心模块 19 个 RESTful API：
       * `01. 身份认证 (Auth)`：`/api/auth/login`、`/api/auth/register`、`/api/auth/me`
       * `02. 核心词典 (Dictionary)`：`/api/dict/search`、`/api/dict/{lemma}`、`/api/dict/id/{id}`
       * `03. 大纲词书 (Wordbook)`：`/api/wordbooks`、`/api/wordbooks/{id}`、`/api/wordbooks/{id}/words`、`/api/wordbooks/{id}/import-to-vocab`
       * `04. 个人生词卡片 (Vocabulary)`：`/api/vocab/cards`、`/api/vocab/cards/{id}/mark-known`、`/api/vocab/overview`、`/api/vocab/cards/{id}`
       * `05. 间隔复习调度 (Review)`：`/api/review/queue`、`/api/review/rating`、`/api/review/today-summary`
       * `06. 研习打卡与热力统计 (Stats)`：`/api/stats/heatmap`、`/api/stats/overview`
     * 在线 Swagger UI 可视化地址：`http://localhost:8080/swagger-ui.html`
     * 机器可读 OpenAPI 3 标准 JSON 地址：`http://localhost:8080/v3/api-docs`
  5. **工程构建与自动化端到端测试**：
     * Maven clean compile：64 个 Java 源文件全部通过编译，`BUILD SUCCESS`；
     * Spring Boot 3 服务在 2 秒内极速启动监听 8080 端口；
     * 执行 7 项全链路自动化测试脚本：从用户登录换取 JWT、词典前缀搜索、大纲词书查询、复习队列提取、FSRS-4.5 评分打卡调度状态跃迁、365 天热力图计算到今日统计摘要，**100% 全部通过**。
* **当前做到哪一步了（Current State Checkpoint）**：
  * 后端框架全量构建完毕，MySQL 8 库表数据正常运转，FSRS-4.5 算法调度无误，OpenAPI 3 接口文档在线就绪。
* **下一步该做什么（Next Steps Action Plan）**：
  1. 将前端与后端 REST API 联调打通（统一前端 `api-client` 请求拦截器附带 Bearer Token）；
  2. 实现词书列表、词条导入、闪卡复习与 FSRS 四档评分前端与后端联动；
  3. 将个人学习热力图与统计指标与后端实际数据实时绑定。

---

### 🟢 记录点 #12：独立研发 3D 单词/字母粒子旋转球体 Demo（Particle Word Sphere）
* **时间**：2026-09-12
* **执行角色**：3D 视觉与图形学工程师 & 前端主程
* **本阶段做了什么（What Was Done）**：
  1. **响应用户诉求（登录页球形动画升级与 ParticleText 灵感融合）**：
     * 响应用户指令：“在这之前先优化登录页面左侧的球形动画，参考 Particle Word Voice 网页的效果，用单词或者字母来进行球形的单词旋转，先写一个独立的 demo 看看效果”；
     * 深入分析参考项目底层数学原理（Fibonacci 球面黄金分割角分布、3D 空间透视投影、屏幕像素网格采样与弹簧阻尼微积分质点模型）；
     * 打造专属于二语习得认知风格的 **3D 单词/字母粒子球体（Particle Word Sphere）**。
  2. **双层复合渲染与物理交互引擎实现**：
     * **第一层：3D 旋转词汇球（Fibonacci Lexical Sphere）**：
       * 精选 70+ 个高频学术/认知词汇（`ephemeral`, `serendipity`, `cognition`, `retrieval`, `resonance`, `neuroplasticity` 等）及核心国际音标（`/æ/`, `/ə/`, `/θ/`, `/ŋ/` 等）与希腊词根符号；
       * 基于黄金分割球面算法精确映射到 3D 球体表面；
       * 真实三维透视投影（$Z$ 轴深度动态缩放、透明度渐变衰减、景深微倾斜角度旋转）；
       * **鼠标 3D 拖拽与惯性动量**：按住画布任意滑动即可自由全向旋转球体，松开后具备真实的物理阻尼旋转；
       * **词汇悬浮高亮与音标释义卡片**：鼠标靠近前排单词时立即触发天蓝微光高亮放大，并动态浮现毛玻璃卡片（显示英文、国际音标与中文精准释义）。
     * **第二层：星云粒子流与字形汇聚（Particle Text Morphing）**：
       * 在球体周围环绕 3,600+ 颗晶莹的微星粒子流；
       * **粒子字形汇聚形态**：支持一键选择预设词（`LEXIFLOW`, `FSRS-4.5`, `MEMORY`, `COGNITION`, `SHADOWING`）或在下方输入框键入任意英文词汇，球体粒子将如星云坍缩般平滑汇聚成高清晰度的发光英文字形；
       * **粒子斥力物理场**：在字形汇聚状态下，鼠标在文字上方移动会产生灵敏的粒子扩散排斥波纹与阻尼回弹（1:1 还原 ParticleText 精髓）；
       * **双击瞬间还原**：双击画布任意位置或点击「还原球体」按钮，粒子星云瞬间重新绽放回 3D 旋转词汇球体。
  3. **双形态原型交付**：
     * **全屏独立极速 HTML 原型**：`public/sphere-demo.html`（通过 `http://localhost:96/sphere-demo.html` 瞬时加载）；
     * **Next.js 独立路由与组件化封装**：
       * 核心组件：`src/components/auth/particle-word-sphere.tsx`（自适应容器、支持响应式 ResizeObserver）；
       * 独立页面路由：`src/app/(dashboard)/sphere-demo/page.tsx`（`http://localhost:96/sphere-demo`）。
  4. **工程构建与连通性验证**：
     * 执行 `pnpm build`，全量 24 个页面在 4.3 秒内 100% 成功预编译，TypeScript 0 报错；
     * Nginx 96 端口联调验证通过：
       * `http://localhost:96/sphere-demo.html` $\to$ **`200 OK`**；
       * `http://localhost:96/sphere-demo` $\to$ **`200 OK`**。
* **当前做到哪一步了（Current State Checkpoint）**：
  * 独立的 3D 单词/字母粒子旋转球体 Demo 已圆满交付，支持直接在浏览器中全屏查看与交互体验。
* **下一步该做什么（Next Steps Action Plan）**：
  * **任务 1（用户验收与效果评估）**：由用户在浏览器体验 3D 旋转与粒子汇聚交互，收集反馈或确认合入 `/sign-in` 登录页左侧替换原地球模型；
  * **任务 2（后端微服务架构设计与数据库配置）**：待登录页动画验收后，正式启动阶段 2 Python FastAPI 后端与本地 SQLite/ECDICT 词典导入。

---

### 🟢 记录点 #11：重构左下角个人菜单与系统设置/帮助反馈全量深度中文化
* **时间**：2026-09-12
* **执行角色**：UI/UX 设计师 & 前端主程
* **本阶段做了什么（What Was Done）**：
  1. **侧边栏结构精简与个人菜单收口归入**：
     * 响应用户需求：“把系统设置和帮助与反馈归入到左下角个人信息弹出的菜单中”；
     * 移除 `src/components/app-sidebar.tsx` 底部多余独立的 `NavSecondary` 挂载，使侧边栏底部视野大幅提纯、极简留白；
     * 在 `src/components/nav-user.tsx` 用户下拉菜单中，将 `系统设置`（`/settings`，带 `SettingsIcon`）与 `帮助与反馈`（`/support`，带 `LifeBuoyIcon`）深度整合归入，与 `个人中心`、`今日研习概览`、`切换账号`、`退出登录` 构成完整的账户偏好与支持中枢。
  2. **系统设置页（/settings）全量中文化与金融模版残余剔除**：
     * 彻底中文化账号安全（修改登录密码、双重身份验证 2FA、活跃登录设备与下线操作）；
     * 研习提醒中文化并深度契合语脉业务（FSRS 抗遗忘临界唤醒、每日研习打卡提醒、系统桌面即时推送、数据安全通知、每周研习认知周报）；
     * 界面外观中文化（极简纸感浅色、黑曜石深色、跟随系统外观）；
     * 彻底清除旧模版残余的 `BillingTab`（银行卡绑定、Visa 卡片、美元订阅等），保持代码库与视觉体验 100% 聚焦二语习得与算法。
  3. **帮助与反馈页（/support）全量算法业务级中文化**：
     * 重构 `src/data/seed.ts`：将原本金融问答全量替换为真实《语脉》核心技术问答（FSRS-4.5 衰减模型、YouTube/B站原声切片精听、影子跟读声学对齐、离线优先与 ECDICT、语境串词生文）；
     * 全面汉化问答分类过滤器、工单处理历史、在线 AI 助教对话器（语脉 AI 研习助教，实时解答疑问与极速打字模拟）；
     * 技术工单提交表单（工单主题、问题分类、优先级、联系邮箱、详细说明、提交状态）；
     * 算法引擎状态大屏（FSRS 调度引擎、Whisper 原声对齐服务、视频精听转录流水线、ECDICT 离线词典数据库、神经网络 TTS 音色合成服务，过去 90 天可用率与状态色块监控）。
  4. **工程编译与代理验证**：
     * 执行 `pnpm build`，全量 23 个静态路由 100% 编译成功，TypeScript 0 警告 0 报错；
     * Nginx 96 端口反代联调验证全部路由正常响应 HTTP 200 OK：
       * `http://localhost:96/settings` $\to$ **`200 OK`**
       * `http://localhost:96/support` $\to$ **`200 OK`**
       * `http://localhost:96/profile` $\to$ **`200 OK`**
       * `http://localhost:96/sign-in` $\to$ **`200 OK`**。
* **当前做到哪一步了（Current State Checkpoint）**：
  * 左下角个人信息弹出菜单完全收口并包含系统设置与帮助反馈；
  * 设置中心与帮助支持中心已 100% 完成本地化与语脉业务契合，无任何英文残余。
* **下一步该做什么（Next Steps Action Plan）**：
  * **任务 1（Git 仓库全量推送）**：将 Checkpoint #11 提交并推送到 GitHub 远程仓库；
  * **任务 2（后端微服务架构设计与数据库配置）**：启动后端 Python/FastAPI 算法微服务与本地 SQLite/DuckDB 数据库初始化。

---

### 🟢 记录点 #10：完善登录体系与个人信息下拉菜单（支持切换账号与退出登录）
* **时间**：2026-09-12
* **执行角色**：系统架构师 & 前端主程
* **本阶段做了什么（What Was Done）**：
  1. **明确登录认证路径与双路由重定向**：
     * 系统登录入口固化为 **`/sign-in`**（支持 `http://localhost:96/sign-in` 直接访问）；
     * 在 `next.config.ts` 中配置 `redirects` 重定向策略，将常用直觉路径 **`/login`** 永久重定向（308）至 **`/sign-in`**，将 **`/register`** 重定向至 **`/sign-up`**；
     * 全面更新登录页为 **《语脉 · LexiFlow》** 官方品牌视觉与二语习得名言，提供流畅的登入反馈与自动跳转至 `/dashboard`。
  2. **重塑左下角个人信息下拉菜单（`NavUser`）**：
     * 将菜单项字段由原本冗长的 **“个人中心与热力图”** 精简规范为 **“个人中心”**；
     * 新增账户管理操作组（带分隔线与标准语义图标）：
       * **切换账号**（`UsersIcon`，直达登录/切换界面）；
       * **退出登录**（`LogOutIcon`，采用 Destructive 警示语义色，安全登出并回到登录页）。
  3. **工程编译与代理验证**：
     * 执行 `pnpm build`，全量 23 个页面在 6.3 秒内 100% 编译成功，TypeScript 0 报错；
     * 通过 Nginx 96 端口反代分别验证：
       * `http://localhost:96/sign-in` $\to$ **`200 OK`**；
       * `http://localhost:96/login` $\to$ **`308 Redirect to /sign-in`**；
       * `http://localhost:96/dashboard` $\to$ **`200 OK`**。
* **当前做到哪一步了（Current State Checkpoint）**：
  * 用户鉴权路径与侧边栏用户交互闭环完全打通，字段精炼规范。
* **下一步该做什么（Next Steps Action Plan）**：
  * **任务 1（Git 仓库全量推送）**：将 Checkpoint #10 提交并推送到 GitHub 远程仓库；
  * **任务 2（后端微服务架构设计与数据库配置）**：正式启动 Python FastAPI 后端与本地 SQLite/DuckDB 数据库初始化。

---

### 🟢 记录点 #09：全景数据彻底移除碎片化小图表，置顶金融级复合大屏
* **时间**：2026-09-12
* **执行角色**：系统架构师 & 前端主程
* **本阶段做了什么（What Was Done）**：
  1. **响应用户关键决策（杜绝小图表碎片化堆砌）**：
     * 响应用户明确指令：“不要再第一行构建这种小图表，这样不仅信息量体现不出来而且堆砌起来得不到任何信息”；
     * 从全景数据布局（`dashboard-customizer.tsx`）中**彻底废除并移除 `AnalyticsKpiGrid` 首行 4 张小图表卡片**，杜绝无意义的碎片化视觉干扰。
  2. **全面强化全景复合大屏（`LearningVelocityCompositeChart`）**：
     * 将其直接置顶作为全景数据模式的第一视觉核心（Row 1 Hero）；
     * 将四大关键宏观算法指标（长期记忆 2,610 词、半衰期 14.8 天、语境切片 84 条、跟读音准 92.4 分）**以内联纯数字标牌（Pure Typography & Crisp Badges）形式**无缝集成在复合大屏顶部，零多余小图表，字体清晰有力；
     * 下方承接完整的双 Y 轴 Recharts `<ComposedChart>`（日研习通量柱状图 + 记忆保持率折线 + 心流专注渐变带），呈现专业大屏的数据厚度与算法说服力。
  3. **布局状态自动升级**：
     * 本地存储键升级至 `lexiflow-dashboard-order-v6`，无需用户清除浏览器缓存即可自动应用全新纯净布局。
  4. **构建与代理联调**：
     * 执行 `pnpm build`，全量 23 个页面在 6.0 秒内 100% 成功预编译，TypeScript 0 报错；
     * Nginx 96 端口反代联调通过，`http://localhost:96/dashboard` 状态 `HTTP/1.1 200 OK`。
* **当前做到哪一步了（Current State Checkpoint）**：
  * 首屏小图表堆砌感彻底消除，取而代之的是开阔、大气、一体化的金融级复合图表大屏。
* **下一步该做什么（Next Steps Action Plan）**：
  * **任务 1（Git 仓库全量推送）**：将 Checkpoint #09 提交并推送到 GitHub 远程仓库；
  * **任务 2（后端微服务架构设计与数据库配置）**：正式启动 Python FastAPI 后端与本地 SQLite/DuckDB 数据库初始化。

---
* **时间**：2026-09-11
* **执行角色**：系统架构师 & 前端主程
* **本阶段做了什么（What Was Done）**：
  1. **全景数据深度提纯（彻底剥离极简任务卡）**：
     * 响应用户反馈，在全景数据（Analytics Mode）中彻底剔除属于极简心流的 `HeroMissionCard`（“把今天该记住的，交给今天的你”文字大卡），避免认知冗余与跨模式元素混淆；
     * 极简心流（Zen Mode）保留该卡，严格保持“极简更极简、全景更硬核”的纯粹双态边界。
  2. **上线多图复合看板（`LearningVelocityCompositeChart`）**：
     * 研发全新的 **全景研习流速与记忆留存复合图谱**（`learning-velocity-composite.tsx`），采用 Recharts `<ComposedChart>` 架构；
     * **双 Y 轴复合联动**：左轴监控每日研习通量（新词摄入 + 稳态复习堆叠柱状图 `Bar`，0~80 词），右轴监控动态记忆保持率折线 `Line`（90%~100%），底部铺设心流专注度柔和渐变区域 `Area`；
     * 标定 `95% 黄金记忆保持线` 虚线基准，内置「多图复合 / 研习通量 / 保持率轨迹」三重视角瞬时切换；
     * 顶部集成 4 列金融级指标标牌（日均通量 63.4 词、综合保持率 96.8%、专注心流指数 94.2、FSRS 负荷均衡 98.2%），底部搭载算法错峰调度诊断 Callout。
  3. **重塑顶部四大 KPI 微图（消除重复单调，注入高密度信息）**：
     * 彻底废除原本 4 张卡片千篇一律的简单斜线 Sparkline；
     * **卡片 1（长期记忆词汇量）**：引入 **三段式记忆成熟度分布微型条**（极熟 1,580 · 稳态 730 · 初稳 300）；
     * **卡片 2（平均记忆半衰期 S）**：引入 **4 次复习间隔拉长微型阶梯柱**（R1 1.8d $\to$ R2 4.2d $\to$ R3 8.6d $\to$ R4 14.8d），直观呈现 FSRS 记忆拉伸效应；
     * **卡片 3（语境切片采摘网络）**：引入 **多源渗透微型堆叠条与来源胶囊**（YT 35条 · B站 24条 · 外刊 15条 · 词书 10条）；
     * **卡片 4（影子跟读声学评分）**：引入 **Whisper 12 根声学音素多峰波形柱**，动态模拟原声对齐与共振峰匹配度。
  4. **工程构建与代理校验**：
     * 执行 `pnpm build`，全量 23 个页面在 6.8 秒内 100% 静态预编译通过，TypeScript 0 报错；
     * 通过 Nginx 96 端口反代验证 `http://localhost:96/dashboard`，返回 `HTTP/1.1 200 OK`。
* **当前做到哪一步了（Current State Checkpoint）**：
  * 全景数据大屏不仅具备顶尖的复合多图联动能力，而且所有微型图表各司其职、信息密度极高，彻底解决了“图表重复、缺乏信息”的问题。
* **下一步该做什么（Next Steps Action Plan）**：
  * **任务 1（Git 仓库全量推送）**：将 Checkpoint #08 提交并推送到 GitHub 远程仓库；
  * **任务 2（后端微服务架构设计与数据库配置）**：启动后端 Python FastAPI 微服务，初始化本地 SQLite/DuckDB 数据库与词库表。

---
* **时间**：2026-09-11
* **执行角色**：系统架构师 & 前端可视化主程
* **本阶段做了什么（What Was Done）**：
  1. **全景数据架构规划与金融级数据大屏美学对标**：
     * 借鉴 `shadcn-fintech` 与 `Shadboard Analytics` 的顶尖设计语汇，遵循 Notion / Vercel 黑曜石单色（Obsidian Monochrome）体系；
     * 将“全景数据模式”升级为极具科技感、算法说服力与学术严谨度（“唬人且专业”）的二语习得全景数据中枢。
  2. **算法 Mock 数据引擎扩充**（`src/data/mock-lexiflow.ts`）：
     * 新增 `mockAnalyticsKpi`：长期记忆词汇量（2,610词）、平均半衰期S（14.8天）、语境切片密度（84条）、影子跟读声学评分（92.4分）及对应 SVG 微型 Sparkline 折线轨迹；
     * 新增 `mockFsrsRetentionData`：30 天周期的 FSRS-4.5 遗忘曲线对抗艾宾浩斯自然遗忘衰减基线数据；
     * 新增 `mockRadarData`：SLA（二语习得）6 维声学与认知雷达（原声辨析、长难句、拼写记忆、跟读音准、考纲覆盖、记忆稳定性）；
     * 新增 `mockCognitiveLoadData`：14 天稳态复习（Review）、新词摄入（New）、遗忘重学（Lapse）认知负荷堆叠数据；
     * 新增 `mockCorpusDistributionData`：YouTube 播客、B站演讲、外刊阅读、考纲词书的多模态语境切片分布比例。
  3. **五大高维可视化图表组件研发**（`src/components/dashboard/`）：
     * `analytics-kpi-grid.tsx`：4 列金融级 KPI 矩阵卡片，内嵌极简无依赖 SVG Sparkline 折线与微型胶囊 Badge；
     * `fsrs-retention-chart.tsx`：FSRS 记忆保持率面积图，支持 7天/30天双周期无缝切换，高亮标注 90% 期望召回目标阈值虚线与抗衰增益；
     * `multimodal-radar-chart.tsx`：6 维多模态 SLA 认知雷达图，直观对比当前能力与同级学习者均值；
     * `cognitive-load-chart.tsx`：14 天认知负荷堆叠柱状图，实时监控每日研习通量与遗忘反弹率；
     * `corpus-donut-chart.tsx`：语境切片环形图，中心内嵌高对比度切片总数（84条）及来源渗透占比明细表。
  4. **仪表盘双模式无缝装配**（`dashboard-customizer.tsx`）：
     * 「极简心流 (Zen)」与「全景数据 (Analytics)」双态并存，存储状态自动升级至 `v4`；
     * 全景模式下所有数据卡片均支持基于 `@dnd-kit` 的拖拽重排与一键重置。
  5. **工程构建与代理校验**：
     * 执行 `pnpm build`，全量 23 个路由 100% 静态预编译通过，TypeScript 0 报错；
     * 通过 Nginx 96 端口反代验证 `http://localhost:96/dashboard`，返回 `HTTP/1.1 200 OK`。
* **当前做到哪一步了（Current State Checkpoint）**：
  * 今日学习概览的双态设计彻底成型：平时极简无压心流背词，需要展示/汇报/复盘时一键切换为震撼的全景算法看板。
* **下一步该做什么（Next Steps Action Plan）**：
  * **任务 1（Git 仓库全量推送）**：将全套可视化代码与日志提交并推送到 GitHub 远程仓库；
  * **任务 2（后端微服务与数据库初始化）**：开始设计 Python FastAPI 后端架构与本地数据库。

---
* **时间**：2026-09-11
* **执行角色**：系统架构师 & AI 算法工程师
* **本阶段做了什么（What Was Done）**：
  1. **战略需求确立与设计方案固化**：
     * 响应用户提议，将“将词库中的待复习词生成故事/文章在语境中研习（Story Mode · 语境串词生文）”作为**核心杀手级 AI 创新特性**正式立项；
     * 明确认知语言学价值：打破孤立背词的“字典效应”，借助连贯叙事建立情节记忆（Episodic Memory），大幅提升词汇在长文中的理解迁移率；
     * 明确大赛价值：紧扣《第八届全球校园人工智能算法精英大赛》方向（一）“开源赋能的 AI 应用创新”，作为算法深度与自研壁垒的核心展示模块。
  2. **系统性算法架构与实施路径设计**：
     * **模块 1（语义聚类与拓扑选择）**：基于开源 Embedding（如 `bge-small-en`）计算今日 FSRS 队列待背 15~20 词的语义余弦相似度，避免生词生硬拼接；
     * **模块 2（CEFR 难易度控制与背景词过滤网）**：基于 Flesch-Kincaid 公式与高频词典，严格限定文章骨架背景词难度（如限 CET-4 以内），确保读者专注掌握 CET-6/考研目标词；
     * **模块 3（受约束结构化生成与字符 Offset 定位）**：以 JSON Schema 约束输出各段落及生词精准字符偏移量，确保目标词 100% 出现且词性准确；
     * **模块 4（后验验证与自纠错循环）**：自检生词完整度与语法合理性，未达标自动微调重写；
     * **模块 5（TTS 朗读与 FSRS 记忆反馈闭环）**：在前端 `/reading` 阅读器中提供双语对照与真人级伴读，读完自动为涉及生词打入一次正面记忆反馈，回写 FSRS 状态。
  3. **产品与研发文档全量更新归档**：
     * 更新 `docs/语脉LexiFlow-产品需求文档.md`：新增「功能 10：按每日待复习词量身定制短文（Story Mode）」P1 级详细规范；
     * 更新 `docs/02-研发路线图.md`：在 S3（AI 增强层）增加 Story Mode 约束生成引擎，在 S4（阅读库）增加专属阅读流；
     * 更新 `docs/04-页面与信息架构.md` 与项目实施计划。
* **当前做到哪一步了（Current State Checkpoint）**：
  * 产品定义、算法链路、技术要点与前后端契约已白纸黑字严格固化到产品规范与路线图中；
  * 前端沉浸式阅读器骨架（`/reading`）已就绪，等待后续后端 Python/FastAPI 微服务接入算法逻辑。
* **下一步该做什么（Next Steps Action Plan）**：
  * **任务 1（Git 同步）**：将更新后的需求文档与路线图提交并推送至 GitHub；
  * **任务 2（后端微服务骨架初始化）**：开始设计后端架构，配置 SQLite/DuckDB 本地数据库存储与 ECDICT 词典表初始化，预留 `/api/v1/generate/story` 接口。

---

### 🟢 记录点 #05：合并今日任务与主词书卡片为一体，热力图移至个人中心（1:1 还原 GitHub 贡献图）
* **时间**：2026-09-11
* **执行角色**：UI/UX 设计师 & 前端主程
* **本阶段做了什么（What Was Done）**：
  1. **左右双卡片合二为一（结构级整合）**：
     * 响应用户反馈，将原本左右分裂的「黑卡：今日研习任务」与「白卡：当前主词书进度」重构合并为一张**一体化全宽黑曜石看板（Unified Hero Mission Card）**；
     * **顶部元信息条**：集成 FSRS 排期状态胶囊与主词书胶囊标签（`📖 CET6 核心高频词汇 · 42%`，带快速跳转与详情入口）；
     * **主体文案**：“把今天该记住的，交给今天的你”与统一的记忆间隔衰减模型说明；
     * **双列指标整合**：
       * 左侧：今日队列三大指标（47 到期复习 / 20 新词研习 / 26m 预计耗时）；
       * 右侧：嵌入式主词书进度面板（42% 已掌握、2,610/6,218 词、精细进度条、预计完成日期、一键切换与每日量调节）；
     * **底部操作栏**：`[ 开始今日研习 (Space) ]` 按钮 + 69% 动态环形进度条，左右不再突兀割裂，视野极其开阔整洁。
  2. **热力图从首页抽离，移植至个人中心（GitHub 级贡献图体验）**：
     * 从首页大盘中彻底移除占据大面积网格的热力图，首页只留核心任务、1 条精选原声切片与快捷底座，实现极致呼吸感；
     * 封装全新的 `src/components/profile/github-heatmap.tsx`：
       * **52 周完整年度日历矩阵**（364 天网格，带 1~12 月顶部月份刻度与周一/周三/周五星期指示）；
       * **悬浮提示 Tooltip**：悬停任一格子显示精确日期与复习反馈次数（如 `📅 2026-05-08: 14 次 FSRS 记忆反馈`）；
       * **年度切换与图例**：支持 2026/2025 年份切换与少-多五档对比度；
       * **个人高光统计**：下方配套 4 大成就卡片（12 天当前连击、28 天最高连击、94.2% 长期记忆留存率、84 个采词总量）。
  3. **新增 `/profile` 个人中心独立路由**：
     * 创建 `src/app/(dashboard)/profile/page.tsx`，展示个人名片（@linz / 离线优先工作区）与 GitHub 贡献热力图；
     * 同步更新设置页（`src/components/settings/settings-page-client.tsx`）的「个人中心」选项卡，挂载该热力图；
     * 侧边栏底部用户头像菜单的「个人中心与热力图」直达 `/profile` 路由。
  4. **构建与连通性验证**：
     * 执行 `pnpm build`，全量 23 个静态路由（包含新增的 `/profile`）在 6.3 秒内 100% 编译通过；
     * PowerShell 联调实测 `http://localhost:96/dashboard`、`http://localhost:96/profile` 与 `http://localhost:96/settings` 全部返回 HTTP 200 OK。
* **当前做到哪一步了（Current State Checkpoint）**：
  * 首页完成了从“信息堆叠”到“极致呼吸留白”的蜕变，核心看板浑然一体；
  * 个人中心拥有了极具极客感与成就感的 GitHub-style 52 周打卡贡献热力图。
* **下一步该做什么（Next Steps Action Plan）**：
  * **任务 1（Git 仓库全量同步）**：提交并推送 Checkpoint #05 源码；
  * **任务 2（后端微服务骨架与数据库）**：开始设计后端 Python/FastAPI 架构与 SQLite/DuckDB 本地存储，对接 ECDICT 本地毫秒检索与 FSRS 记忆间隔计算算法。

---

### 🟢 记录点 #04：视觉全面升级为 Notion/Vercel 黑白极简克制风，上线首页极简心流/全景数据双模式
* **时间**：2026-09-11
* **执行角色**：UI/UX 设计师 & 前端架构师
* **本阶段做了什么（What Was Done）**：
  1. **主题色全面重构（Notion / Vercel 黑白极简无色）**：
     * 响应用户对原森林绿配色的优化诉求，将视觉风格升级为顶级生产力工具推崇的**极度克制黑白无色系统**；
     * 修改 `src/app/globals.css`：
       * 主背景采用瓷白纸感冷灰 `#FAFAFA`（深色 `#09090B`）；
       * 卡片表面采用纯净表面 `#FFFFFF`（深色 `#121215`）；
       * 强调色升级为深曜黑 `#09090B`（深色纯白 `#F8FAFC`），搭配细腻石板锌灰 `#E4E4E7` 分割线；
       * 彻底剔除刺眼色彩噪点，达到“信息与文字至上、零视觉疲劳”的沉浸式研习质感。
  2. **首页信息密度大降噪（上线 Zen / Analytics 双模式切换）**：
     * 针对原首页信息过密、视觉压迫的问题，实施**渐进式信息暴露（Progressive Disclosure）**方案：
     * **默认极简心流模式（Zen Mode）**：
       * 移除冗余的背景波纹与密集卡片堆叠；
       * 仅保留核心的 `HeroMissionCard`（到期复习 47、新词 20、预计用时 26m，按 `Space` 3 秒直达背词）；
       * 新增 `DailyCuratedQuote`（今日精选原声高光句与重点生词切片，支持一键试听）；
       * 底部搭配极简轻量 `QuickActionDock`；页面大量留白，呼吸感大幅提升。
     * **全景数据模式（Analytics Mode）**：
       * 用户点击右上角 `[ 极简心流 (Zen) | 全景数据 (Analytics) ]` 即可秒级切换；
       * 平滑展开 CET-6 主词书详尽掌握度、13 周学习热力图与完整语境快照流，并保留自由拖拽布局能力；
       * 用户模式选择通过 LocalStorage 自动记忆。
  3. **侧边栏与核心组件同步重塑**：
     * 侧边栏 `AppSidebar` 徽标、Streak 连击卡片与徽章全量同步为纯黑曜石高对比现代质感；
     * 视频字幕同步高亮、生词本卡片全量去绿色化，呈现统一高级的排版标准。
  4. **工程构建与校验**：
     * 执行 `pnpm build`，全量静态路由在 6.5 秒内 100% 成功生成，0 编译警告与错误；
     * 验证 `http://localhost:96` 代理服务持续稳定在线。
* **当前做到哪一步了（Current State Checkpoint）**：
  * 前端整体视觉质感完成质的飞跃，信息层级疏密有致，既满足初入页面时的“极简零负担”，又兼顾深度用户的“全景统计需求”。
* **下一步该做什么（Next Steps Action Plan）**：
  * **任务 1（Git 仓库全量同步）**：提交并推送 Checkpoint #04 的最新源码；
  * **任务 2（后端微服务骨架与数据库）**：开始设计后端架构，配置 SQLite/DuckDB 本地数据库存储与 ECDICT 词典表初始化。

---

### 🟢 记录点 #03：前端全栈深度二次开发完成，Nginx 96 端口反代就绪
* **时间**：2026-09-11
* **执行角色**：系统架构师 & 前端主程
* **本阶段做了什么（What Was Done）**：
  1. **Nginx 反向代理配置与常驻服务**：
     * 修改本地 Nginx 配置（`D:\devtools\nginx-1.20.2\conf\nginx.conf`），新增监听 96 端口的反向代理块；
     * 将 `http://localhost:96/` 代理至 Next.js 前端服务（`127.0.0.1:3000`），支持 WebSocket 热重载头协议；
     * 预留后端 FastAPI 算法微服务代理路由 `/api/v1/`（指向 `127.0.0.1:8000`）；
     * 在项目中保存 `deploy/nginx/lexiflow-port96.conf` 部署脚本，Nginx 重载成功且 96 端口监听验证通过。
  2. **现代纸感编辑风设计令牌与排版装配**：
     * 按照 `docs/06-设计风格与UI规范.md` 重构 `src/app/globals.css`：
       * 冷白纸底 `#F4F6F2`、纯净卡片表面 `#FBFCF9`、深松墨字 `#1D2523`；
       * 森林松绿主品牌色 `#1D9384`、暖杏金辅助色 `#D68E5A` 与柔和微衬色；
     * 在 `src/app/layout.tsx` 引入 Google Fonts `Manrope`（正文）+ `DM Mono`（音标/时间戳/指标）双字体引擎，杜绝廉价 AI 感。
  3. **导航系统与 App Shell 重构**：
     * 重建 `src/components/app-sidebar.tsx` 与 `nav-main.tsx`：
       * 品牌名固化为 **语脉 · LexiFlow**（多模态语境研习，带 `BETA` 徽标）；
       * 结构划分为“研习主线”（今日概览、词书库、生词本）与“真实语境”（视频精听、阅读库）；
       * 侧边栏集成 **Streak 连续研习连击卡片**（🔥 连续 12 天 · 今日进度 69%）；
     * 升级 `src/components/command-palette.tsx`（`⌘K` 全局检索），支持秒级搜词、词书与视频；
     * 适配 `src/components/dynamic-breadcrumb.tsx` 全局中文面包屑。
  4. **今日学习概览大厅（Dashboard）深度二开**：
     * 创建 `src/data/mock-lexiflow.ts` 算法级高质量 Mock 数据引擎；
     * 实现 `HeroMissionCard`：深邃松绿底、69% 动态环形进度条、47 到期复习 / 20 新词 / 26m 预计用时、支持空格键（`Space`）一键启动会话；
     * 实现 `ActiveWordbookCard`：CET-6 核心词汇 42% 进度看板与目标日期预测；
     * 实现 `RecentCaptures`：多模态采词流（视频/文章来源标签、原生音频切片试听、重点词高亮）；
     * 实现 `LearningHeatmap`：13 周研习节奏热力网格与单日复习反馈 Tooltip；
     * 实现 `QuickActionDock`：心流快捷操作底座（查词、导入视频、打开统计）；
     * 优化 `DashboardCustomizer`：支持拖拽定制、排序记忆与一键恢复默认。
  5. **四大核心业务路由骨架交付**：
     * `/wordbooks`：词书库（多分类筛选、进度指示、设为主词书）；
     * `/vocab`：生词本（ECDICT 释义、真实语境句子高亮、FSRS S/D 参数与星标）；
     * `/videos`：视频精听工作台（URL 粘贴解析栏、Whisper 离线加速对齐与当前句高亮三态、影子跟读评测交互入口）；
     * `/reading`：沉浸式阅读库（杂志级排版、分级阅读、划词秒级弹窗释义）。
  6. **编译验证与联调**：
     * 运行 `pnpm build`，Next.js 16 (Turbopack) 22 个静态路由全量编译通过（0 报错）；
     * 启动开发服务，通过 PowerShell 验证 `http://localhost:96` 及全部 5 个核心页面返回 HTTP 200 OK。
* **当前做到哪一步了（Current State Checkpoint）**：
  * **【阶段 1：前端工程二开与核心看板】已 100% 圆满交付**；
  * 用户可在浏览器中直接打开 `http://localhost:96` 或 `http://localhost:3000` 体验完整界面的交互与视觉效果。
* **下一步该做什么（Next Steps Action Plan）**：
  * **任务 1（Git 仓库全量同步）**：将新增的所有前端二开源码与配置提交并推送到 GitHub 远程仓库；
  * **任务 2（FSRS 算法接入准备）**：启动后端 Python/FastAPI 算法微服务或 SQLite/DuckDB 本地数据库，对接真实的 ECDICT 词典检索与 FSRS 记忆间隔计算；
  * **任务 3（音视频切片流媒体接口）**：接入 Whisper 离线推理模型，实现真实字幕对齐与发音评测对比。

---

### 🟢 记录点 #02：拉取 Shadcn Fintech 本地参考源码，启动前端研发
* **时间**：2026-09-11
* **执行角色**：系统架构师 & 前端工程师
* **本阶段做了什么（What Was Done）**：
  1. **定位并克隆开源标杆项目**：通过全网检索确定了 `shadcn-fintech.vercel.app` 的官方开源仓库（`abderrahimghazali/shadcn-fintech`）；
  2. **本地源码归档到 `material/`**：成功克隆至 `material/shadcn-fintech`，完整保留了：
     * `src/components/analytics/spending-heatmap.tsx`（52周打卡热力图核心组件）；
     * `src/components/analytics/category-donut.tsx`（分类分布环形图）；
     * `src/components/app-sidebar.tsx`（浮岛式 Inset 侧边栏）；
     * `src/components/command-palette.tsx`（Cmd+K 快捷检索）；
     * `src/app/globals.css`（Tailwind v4 主题与全局变量）。
  3. **出处索引更新**：同步更新 `material/参考资料与引用出处.md`，记录本地源码路径。
  4. **Git 仓库初始化与全量推送**：
     * 在 `AIC` 根目录初始化 Git 仓库与标准 `.gitignore`（过滤 `node_modules` 与参考源码）；
     * 创建正式的项目 `README.md`；
     * 成功对接远程仓库 `https://github.com/jiezoui/LexiFlow.git`，完成首次完整提交并直接推送到 `main` 分支。
* **当前做到哪一步了（Current State Checkpoint）**：
  * 远程仓库 `github.com/jiezoui/LexiFlow` 已正式同步所有规范、PRD、UI 指南与开发日志；
  * 参考源码已安全存放在 `material/shadcn-fintech`；
  * 准备正式创建/初始化《语脉 · LexiFlow》的前端主工程代码。
* **下一步该做什么（Next Steps Action Plan）**：
  * **选型决策确认**：确定前端工程采用 **Next.js (React)** 快速二开（速度最快、无缝复用该模板全部组件）还是按照原规划采用 **Vue 3 + Vite + shadcn-vue** 移植重构；
  * **创建前端工程目录**：初始化前端代码骨架；
  * **注入《语脉》视觉体系**：将调色板替换为森林松石绿（`#1D9384`）与暖杏金（`#D68E5A`），替换导航项与文字，启动本地开发服务。

---

### 🟢 记录点 #01：项目立项、需求对齐与设计基线锁定
* **时间**：2026-09-11
* **执行角色**：系统架构师 & AI 全栈工程师
* **本阶段做了什么（What Was Done）**：
  1. **项目深度分析与梳理**：全面解读原有需求文档与代码，提炼“词书系统背诵 + 真实视频语境采词”的“双入口单引擎（FSRS）”核心闭环，锁定“视频原声例句反哺词书”为最核心差异化。
  2. **大赛赛事精准对齐**：深入剖析《第八届全球校园人工智能算法精英大赛》文件，提炼 100 分评分标准与“打击套壳、强调开源融合与工程完成度”的风向标，确定申报方向（一）“开源赋能的 AI 应用创新”。
  3. **品牌与中文名确立**：将项目正式确立为 **“语脉 · LexiFlow”**，紧扣语言学中的“连贯言语（Connected Speech）/ 语流脉络”背景，全面杜绝外部重名。
  4. **全套技术文档中文化归档**：
     * `docs/01-产品定义.md`
     * `docs/02-roadmap.md` $\to$ `docs/02-研发路线图.md`
     * `docs/03-architecture.md` $\to$ `docs/03-技术架构.md`
     * `docs/04-pages.md` $\to$ `docs/04-页面与信息架构.md`
     * `docs/05-算法大赛参赛指南.md`
     * `docs/语脉LexiFlow-产品需求文档.md`（标准 PRD 基线）
     * `docs/原型预览-今日概览.html`
  5. **MCP 工具链安装与配置**：
     * 全局安装并验证了 `reicon-mcp`（数万个离线矢量图标库）；
     * 响应官方文档，全局安装并配置了 `shadcndashboard-mcp`，写入全局 `~/.gemini/config/mcp_config.json`，为后续前端快速拉取后台 UI 组件打通管道。
  6. **顶级 UI 参考调研与借鉴提炼**：
     * 深度抓取并剖析了 `Shadcn Fintech` 与 `Shadboard` 两个后台标杆；
     * 决定将 `Shadcn Fintech` 的 52 周年度活跃度热力图 1:1 移植改造为《语脉》的“每日学习/复习打卡热力图”，并借鉴其浮岛式（`variant="inset"`）侧边栏与等宽数字排版。
  7. **参考资料归档与工程规范确立**：
     * 创建 `material/` 文件夹，生成 `material/参考资料与引用出处.md`，汇总开源项目（ECDICT、FSRS、Whisper、LibreTranslate 等）的出处与许可；
     * 编制 `docs/06-设计风格与UI规范.md`，确立以“冷白纸感底色 + 森林松绿 + 暖杏金”为核心的现代纸感编辑风；
     * 编制 `docs/07-开发规范与工程准则.md`，确立 ArchUnit 架构守卫、三层流转规范与 Git 提交规范。

* **当前做到哪一步了（Current State Checkpoint）**：
  * **【设计与规范准备阶段】已 100% 结束**。
  * 产品的业务逻辑、竞赛切入点、技术选型、数据库设计、UI 设计规范、开源资产引用均已白纸黑字严格固化。
  * 项目目录结构整洁规范，所有参考资料归档完毕，开发环境具备所有必要的 MCP 工具。

* **下一步该做什么（Next Steps Action Plan）**：
  * **任务 1（前端脚手架搭建）**：在项目中初始化 Vue 3 + Vite + TypeScript 前端项目，配置 Tailwind CSS 主题 Token（合入我们的设计变量）以及基础 UI 组件库。
  * **任务 2（首页看板原型还原）**：将 `docs/原型预览-今日概览.html` 与借鉴自 `Shadcn Fintech` 的年度打卡热力图移植为规范的 Vue 3 组件。
  * **任务 3（后端工程初始化）**：创建 Java 21 + Spring Boot 3.3 模块化单体骨架，配置 ArchUnit 依赖测试与 10 个业务模块的包结构。
  * **任务 4（种子数据准备）**：生成首批 500~1000 条包含 CET-4/6 核心词的 Seed 数据初始化 SQL。

### Checkpoint 13：3D 核心词汇球 (Cinematic Lexical Sphere) 1:1 像素级复刻与落地
* **时间**：2026-09-12
* **执行角色**：资深图形算法与全栈工程师
* **本阶段做了什么（What Was Done）**：
  1. **视觉标杆深度拆解**：针对用户提供的参考图（`media_1789145162626.jpg`），剖析出核心视觉要素：
     * **核心居中词 (Hero Word)**：正中央明亮白炽光、粗黑体字重的 `LexiFlow`（支持原图 `Fexiflow` 切换）；
     * **球面曲率水平挤压 (Spherical Curvature Squash)**：计算视线法线夹角余弦，边缘词汇水平挤压 `squashX`，产生紧密贴合球体曲面的透视质感；
     * **切线曲率偏转 (Tangent Contour Tilt)**：靠近球体外边缘的词汇沿着圆环切线微倾，杜绝平铺标牌感；
     * **极密词汇包裹 (Dense Hierarchy)**：扩展至 360+ 个词汇与音标符号，多层错落包裹形成坚实球体轮廓；
     * **斜切轨道行星环 (Planetary Orbit Ring)**：左下至右上 ~ -22° 倾角斜切轨道，分层渲染（Z<0 穿过球后，Z≥0 穿过球前），并附带光子火花与右上发散光芒；
     * **空间微光悬浮方块 (Floating 3D Dust)**：空间中随机漂浮的旋转微型发光立方体与星尘。
  2. **双载体落地与调试面板**：
     * **独立全屏原生 Demo**：`public/sphere-demo.html`（支持右侧微调面板：词汇切换、光环开关、微尘开关、密度档位、自转速度）；
     * **React 生产组件**：`src/components/auth/particle-word-sphere.tsx`，修复了 TypeScript 控制流分析下的类型窄化问题；
     * **登录页左侧融合**：`src/app/(auth)/sign-in/page.tsx` 左侧增加模式切换器（【3D 词汇球】与【经纬地球】），默认呈现电影感 3D 词汇球。
  3. **质量检验与工程闭环**：
     * `pnpm tsc --noEmit` 0 错误通过；
     * HTTP 200 端点验证：`http://localhost:96/sphere-demo.html`、`http://localhost:96/sphere-demo`、`http://localhost:96/sign-in` 全部正常访问。

### Checkpoint 14：登录页面还原与 3D 词汇球原型归档
* **时间**：2026-09-12
* **执行角色**：系统架构师 & 全栈工程师
* **本阶段做了什么（What Was Done）**：
  1. **登录页 1:1 状态回滚**：
     * 响应用户指令，将 `src/app/(auth)/sign-in/page.tsx` 完全恢复至最初的 3D 经纬地球仪（`GlobeDemo`）形态；
     * 移除登录页面左侧的切换按钮及依赖，确保登录页原汁原味。
  2. **3D 词汇球独立资产与原型完整保留**：
     * 原生全屏原型 `public/sphere-demo.html` 依然保留并可用，方便后续需要时随时调用；
     * React 独立组件 `src/components/auth/particle-word-sphere.tsx` 及其独立路由页面 `/sphere-demo` 完好归档。
  3. **质量与构建验证**：
     * `pnpm tsc --noEmit` 严格类型检测 0 错误；
     * 端点验证：`http://localhost:96/sign-in` 返回 HTTP 200，登录页经纬地球仪正常运行。

* **当前做到哪一步了（Current State Checkpoint）**：
  * 登录页面已成功复原为原有状态。

* **下一步该做什么（Next Steps Action Plan）**：
  * 按照之前的既定计划，继续推进系统设置、帮助与反馈的汉化细节以及后续核心业务模块。

### Checkpoint 27：闪卡研习与复习界面紧凑防滚动重构、默写术语规范化与纯净交互架构升级
* **时间**：2026-09-13
* **执行角色**：系统架构师 & 前端交互主程
* **本阶段做了什么（What Was Done）**：
  1. **背新词（LEARN Mode）紧凑防滚动排版**：
     * **四选一全状态完整保留**：严格保留 4 个选项按钮在卡片内（答题前与答题后均完整展示，答题后高亮正确项/选中项），杜绝破坏用户心理认知；
     * **限高防溢出机制**：答题后展开的中文释义与真题例句容器严格设定限高（`max-h-[75px] sm:max-h-[85px] overflow-y-auto`），例句过长时内部平滑滚动；
     * **全视口自适应**：卡片与内边距紧凑化（高度约 340px），配合顶部顶栏总高约 480px，彻底消灭标准笔记本屏幕（768p/1080p@125%/150%）下的纵向页面滚动条。
  2. **巩固复习（REVIEW Mode）防滚动与纯净 3D 卡片架构**：
     * **复习流程纯净化**：彻底移除复习卡片内部的击键模式切换分支及 `[🎴 3D翻转] [⚡ 击键默写]` 冗余切换按钮，正常复习回归纯粹的 3D 翻转闪卡交互，避免用户认知分心；
     * **卡片背面尺寸收缩**：卡片背面语境快照同样设置限高（`max-h-[80px] sm:max-h-[90px] overflow-y-auto`）；
     * **底部 4 档评分按钮紧凑化**：将 FSRS 1-4 档评分按钮压缩至高响应度胶囊格（`p-2 sm:p-2.5 rounded-2xl`，高度仅 ~44px），整体复习区域零滚动呈现。
  3. **默写功能术语通俗化与架构合理化**：
     * 去除“基建默写”、“击键动力学客观考核”等学术华丽词汇，统一规范为通俗直接的**「单词默写」**；
     * 架构合理化：默写不在日常背词卡片中打扰用户，而是作为一组新词学完或一轮复习结束后的阶段结算看板可选加固项（「开始单词默写」）；
     * 默写交互面板平视显示器（HUD）标签通俗化规范：“思考用时”、“输入状态”、“退格修改”、“提示字母”，决策弹窗归一为“默写结果评定”。
  4. **工程质量与构建验证**：
     * 执行 `pnpm build`，Next.js 全量 25 个静态与动态路由 100% 编译通过，TypeScript 0 错误；
     * 通过 Nginx 96 端口验证 `http://localhost:96/cards` 返回 `HTTP/1.1 200 OK`。

* **当前做到哪一步了（Current State Checkpoint）**：
  * 闪卡中心在背新词、巩固复习两个核心阶段均实现 100% 页面无纵向滚动条；
  * 四选一释义选项在背词前后稳定留存；
  * 单词默写从卡片内抽离，统合至阶段完成看板，概念统一通俗，体验流畅连贯。

* **下一步该做什么（Next Steps Action Plan）**：
  * 继续围绕用户在闪卡练习中的后续反馈推进打磨，或者开展影子跟读工坊与词书管理模块的后续联动。

### Checkpoint 28：背新词选项卡高度饱满化与大卡片视口比例美学深度调优
* **时间**：2026-09-13
* **执行角色**：UI/UX 设计师 & 前端主程
* **本阶段做了什么（What Was Done）**：
  1. **四选一选项按钮饱满度回升**：
     * 针对用户反馈“四个框高度压缩太厉害”，将选项按钮从扁平单行（`p-2`）升级为舒展沉浸的高质感卡片（`p-3.5 sm:p-4 rounded-2xl min-h-[52px] sm:min-h-[56px]`）；
     * 字母角标放大至 `size-7 rounded-xl`，字号提升至 `text-xs sm:text-sm font-medium`，兼顾触击容错率与视觉分量感；
     * 间距提升至 `gap-3 sm:gap-3.5 my-2.5 sm:my-3.5`，彻底告别压扁感。
  2. **视口纵向留白与大卡片比例美学优化**：
     * 修复了“显示句子后卡片下边缘和页面底部留白距离过大（空洞感）”的视觉失衡问题；
     * 大卡片引入 `flex-1` 与 `min-h-[440px] sm:min-h-[480px]`，内边距扩充至 `p-5 sm:p-7 md:p-8`，自然向视口下方延展，消除底部大块空白断层；
     * 词汇标题提升至 `text-3xl sm:text-4xl md:text-5xl font-serif font-black`，语境真题例句盒扩充至更舒服的排版空间（`max-h-[110px] sm:max-h-[125px]`），整体庄重典雅、舒展协调。
  3. **构建与端点校验**：
     * 执行 `pnpm build`，Next.js 全量 25 个静态与动态路由 100% 编译通过，TypeScript 0 错误；
     * 96 端口验证 `http://localhost:96/cards` 正常返回 200 OK。

* **当前做到哪一步了（Current State Checkpoint）**：
  * 背新词界面比例平衡，四选一选项按钮既饱满易点，大卡片在展示释义例句后与页面下边界衔接匀称自然。

### Checkpoint 29：背新词答对极速自动流转（900ms）与严格限制回退前 2 词机制深度实现
* **时间**：2026-09-13
* **执行角色**：全栈架构师 & 交互体验工程师
* **本阶段做了什么（What Was Done）**：
  1. **答对极速自动流转逻辑（Auto-Advance on Correct）**：
     * **核心机制**：在背新词模式下，用户若选择正确释义，自动朗读单词强化记忆，并启动 900ms 倒计时定时器直接自动流转跳转至下一个单词，实现行云流水般的背词连贯感；
     * **错误与看详解保护**：若用户答错或点击「不认识 / 看详解」，**坚决不执行自动跳转**，完整保留选项对比、音标、词性、中英文双解释义及真题例句，并执行“先读单词再读例句”的双重语境输入，必须由用户自主确认理解后手动点击「下一个新词（Enter）」跳转；
     * **灵活跳过与防竞争机制**：自动流转过程中提供动态呼吸光效与倒计时提示；用户若不愿等待 900ms，按 `Enter`/`Space` 或直接点击按钮可立即进入下一词；所有跳转、回退、卸载操作均严格清理定时器句柄，杜绝重复提交与竞态冲突。
  2. **严格限制回退前 2 词功能（Strict Limited 2-Word Rewind）**：
     * **严格约束**：在顶部操作条与底部控制栏提供「回退查看已学」按钮，严格将回退深度限制在**最多前 2 个已学单词**（`maxRewindCount = Math.min(2, learnIndex)`，`offset` 范围严格受控于 `[0, -1, -2]`，杜绝无限往前倒翻）；
     * **全真复盘回看体验**：
       * 回看已学单词时，卡片自动朗读该词发音，选项区域以高亮绿色徽标标记正确释义；
       * 展开并展示该词完整词性、双语释义与例句，支持按 `S` 随时重听例句朗读；
       * 顶部清晰显示当前已回退词数标记 `⏪ 回顾已学词模式 (前 N 词 · 上限 2 词)`；
       * 提供「更早前一词（已达上限则禁用）」、「后一词」与显目的「返回当前学习进度」按钮；
     * **键盘快捷键深度融合**：
       * 正常背词时支持按左箭头 `←` 回看上一个词；
       * 回看模式下支持按 `←` 回看更前一词、按 `→` 查看后一词、按 `Enter` / `Space` / `Escape` 瞬间返回当前最新学习进度。
  3. **工程质量与构建验证**：
     * 执行 `pnpm build`，Next.js 全量 25 个路由静态页面构建 100% 成功，TypeScript 0 错误；
     * 通过 Nginx 96 端口验证 `http://localhost:96/cards` 正常返回 `HTTP/1.1 200 OK`。

* **当前做到哪一步了（Current State Checkpoint）**：
  * 背新词界面具备高智能化自适应节奏：答对快速流转，答错沉浸研习；
  * 已学词汇支持随时回退查看（严格限制最多前 2 词），随时一键回归学习进度，闭环完整丝滑。

* **下一步该做什么（Next Steps Action Plan）**：
  * 继续倾听用户在日常刷词与备考过程中的交互反馈，持续打磨细节。

### Checkpoint 30：skywind3000/ECDICT 权威英汉双解词库深度接入与全字段智能适配
* **时间**：2026-09-13
* **执行角色**：系统架构师 & 全栈工程师
* **本阶段做了什么（What Was Done）**：
  1. **ECDICT 原始数据建模与智能适配器（EcdictRawRecord & EcdictAdapter）**：
     * **13 列字段全量建模**：全面映射 skywind3000/ECDICT 规范：`word, phonetic, definition, translation, pos, collins, oxford, tag, bnc, frq, exchange, detail, audio`；
     * **音标规范化与 IPA 国际音标转换**：清除首尾多余括号与斜杠，统一规范为 `/[phonetic]/` 格式；将 ECDICT 中历史西里尔音标字符 `ә`（`\u04d9`）智能转换为标准国际音标 schwa `ə`（`\u0259`），消除字符编码歧义与乱码；
     * **多维词性提取**：联合 `pos` 缩写与 `translation` 释义行前缀正则匹配，精准提取出标准词性标签（如 `n. / vt. / adj.`）；
     * **中英双解释义深度清洗**：保留并格式化中文释义换行排版，剔除无效网络标记与杂质，同时清洗柯林斯英英双解；
     * **分级考纲标签矩阵映射**：将 ECDICT 中的考级缩写精准映射为平台规范标签：`zk`->中考, `gk`->高考, `cet4/6`->CET4/CET6, `ky`->考研, `ielts`, `toefl`, `gre`, `Oxford3000`, `Collins-N★`；
     * **词频排序与权威音频源生成**：优先选用 COCA 词频排名（`frq`），次选英国国家语料库排名（`bnc`）；无自带音频时智能生成有道权威英音/美音双 CDN 试听地址；
     * **真题语境双语例句补充**：优先从 `detail` 提取地道双语例句，无例句时依据词汇语义与真实释义自动补充高质量学术/备考语境例句。
  2. **毫秒级 O(log N) 二分检索与多源降级架构（EcdictServiceImpl）**：
     * **超轻量二分查找**：利用 ECDICT CSV 按单词字母序严格排序特性，采用 `RandomAccessFile` 字节定位与断行保护机制，在 65MB 大文件中实现 <2ms 极速检索，避免将海量词库全量载入 JVM 内存；
     * **高频 LRU / ConcurrentHashMap 热点缓存**：热词毫秒级极速命中；
     * **全量库与样本库平滑降级**：实现 `resolveCandidateFiles` 机制，按优先级自动尝试配置路径、全量库 `material/ecdict.csv` 与精简示例库 `material/ecdict.mini.csv`，在后台静默下载或测试环境下均能平滑运行。
  3. **业务链路全景深度融合**：
     * **核心词典检索流**：`DictionaryServiceImpl.getByLemma` 与 `search` 支持未收录生词动态从 ECDICT 抽取有效字段，适配后自动持久化至 MySQL `dict_entry` 并返回；
     * **词书上传导入流**：`WordbookServiceImpl.uploadWordbookFile` 对用户上传自定义词表中本地未收录的生词，通过 `batchAdaptWords` 批量从 ECDICT 富化权威音标、释义、考级标签与词频；
     * **生词本加入流**：`VocabularyServiceImpl.addCard` 针对新词智能调用 ECDICT 适配器自动建档；
     * **独立调试端点**：提供 `GET /api/dict/ecdict/preview?word=xxx`（预览适配模型）及 `POST /api/dict/ecdict/batch-adapt`（批量适配入库）。
  4. **全套自动化测试与工程验证**：
     * **单元测试**：编写并运行 `EcdictAdapterTest`（4 个测试）和 `EcdictServiceTest`（4 个测试），8 个单元测试 100% 通过（总耗时 5.1s）；
     * **前端构建验证**：执行 `pnpm build`，全量 25 个静态与动态路由构建通过，TypeScript 0 错误；
     * **实机 API 端点验证**：`GET /api/dict/abroad`、`GET /api/dict/inconsequence`、`GET /api/dict/ecdict/preview` 均成功返回 200 OK 与完整的适配模型字段。

* **当前做到哪一步了（Current State Checkpoint）**：
  * skywind3000/ECDICT 词库的字段解析、模型适配、高性能二分检索、热点缓存及全业务链路（查词、生词本、词书导入）全部无缝打通。

* **下一步该做什么（Next Steps Action Plan）**：
  * 等待全量 `ecdict.csv` 完全就绪，并根据用户后续需求持续拓展更多词典特性。

### Checkpoint 31：背新词大卡片与选项框高度按 1/9 精准缩减与视口紧凑调优
* **时间**：2026-09-13
* **执行角色**：UI/UX 设计师 & 前端主程
* **本阶段做了什么（What Was Done）**：
  1. **背新词主卡片高度按 1/9 精准缩减**：
     * 响应用户“学新单词的框再减少 1/9 高度”需求，针对 1080p 笔记本与各种屏幕视口进行精准比例压缩（减少 ~11%）：
     * 容器最小高度从 `min-h-[440px] sm:min-h-[480px]` 缩减至 `min-h-[390px] sm:min-h-[425px]`（严格符合 $480 \times \frac{8}{9} \approx 426\text{px}$）；
     * 卡片内边距从 `p-5 sm:p-7 md:p-8` 紧凑微调为 `p-4 sm:p-6 md:p-7`；
     * 顶部栏底部内边距由 `pb-3` 收紧为 `pb-2.5`，词条标题纵向边距由 `py-2.5 sm:py-3.5` 收紧为 `py-2 sm:py-2.5`。
  2. **四选一选项按钮与例句展示区同步紧凑化（比例和谐度调优）**：
     * 四选一释义选项框最小高度从 `min-h-[52px] sm:min-h-[56px]` 减低为 `min-h-[46px] sm:min-h-[50px]`（符合 $56 \times \frac{8}{9} \approx 49.7\text{px}$）；
     * 选项按钮内边距调整为 `p-3 sm:p-3.5`，网格行间距收紧为 `my-2 sm:my-2.5` 与 `gap-2.5 sm:gap-3`；
     * 字母角标微调至 `size-6 sm:size-6.5`，例句滚动盒最大高度从 `max-h-[110px] sm:max-h-[125px]` 优化为 `max-h-[98px] sm:max-h-[110px]`；
     * 无论展开详解决定还是展开语境真题例句，整张大卡片在各种屏幕下均完美锁死在视口内，毫无滚动条与页面抖动。
  3. **工程质量与构建验证**：
     * 执行 `pnpm build`，Next.js 全量 25 个静态与动态路由 100% 编译通过，TypeScript 0 错误；
     * Nginx 96 端口与 Next.js 3000 端口验证 `http://localhost:3000/cards` 返回 `HTTP/1.1 200 OK`。

* **当前做到哪一步了（Current State Checkpoint）**：
  * 背新词工作台的大卡片与选项框均按约 1/9 比例紧凑化缩减，各元素比例协调典雅，视觉沉浸感与紧凑感达到最佳平衡。

### Checkpoint 32：巩固复习界面独立一键「直接默单词」与键盘快捷键 [D] 深度闭环
* **时间**：2026-09-13
* **执行角色**：全栈架构师 & 交互体验工程师
* **本阶段做了什么（What Was Done）**：
  1. **巩固复习模式专属独立「直接默单词」按钮**：
     * **工具栏常驻按钮**：在模式切换工具栏右侧为巩固复习模式专属添加琥珀色高辨识度独立按钮 `[⌨️ 直接默单词 · N 词]`，点击即可跳过逐张卡片翻转，直接对当前待复习队列开启全真击键拼写默写；
     * **卡片指示栏实时按钮**：在 3D 翻转卡片正上方计数栏 `第 X / Y 张复习卡片` 旁提供精巧按钮 `[⌨️ 直接默单词 D]`，支持随时随地从当前卡片及后续待复习词汇无缝切入击键拼写练习；
     * **无缝桥接击键动力学考核面板**：将复习队列自适应适配并装载至 `KeystrokeCardPanel` 默写工作区，支持实时 WPM 测速、反应时间打标、退格次数遥测、字母线索补全及 FSRS 科学间隔自动调度结算。
  2. **键盘全局快捷键 [D] 融合与操作提示增强**：
     * 键盘监听器中加入大写 `D` 快捷键，在巩固复习页面按下 `D` 键立即呼出单词默写工作台；
     * 翻转卡片顶栏与底栏提示文案同步更新为 `[空格翻转 · D默单词 · W读词]` 与 `[按 1-4 评分 · D默此词 · W读词 · S读句]`，降低认知门槛。
  3. **工程质量与构建验证**：
     * 执行 `pnpm build`，全量 25 个静态与动态路由 100% 编译通过，0 TypeScript 报错；
     * 本地测试 `http://localhost:3000/cards` 返回 `HTTP/1.1 200 OK`，按钮交互与快捷键流转丝滑。

* **当前做到哪一步了（Current State Checkpoint）**：
  * 巩固复习页面已拥有完全独立的「直接默单词」入口与 [D] 快捷键，用户可按需在「3D 翻转抽认卡」与「硬核击键拼写默写」之间自由穿梭。

### Checkpoint 33：背新词闪卡顶栏重构、小喇叭旁控件内聚与「上一个」回退深度优化
* **时间**：2026-09-13
* **执行角色**：UI/UX 设计师 & 前端主程
* **本阶段做了什么（What Was Done）**：
  1. **闪卡内顶栏小喇叭右侧控件重构与冗余文案清理**：
     * 彻底移除了闪卡顶栏小喇叭右侧冗长密集的快捷键提示文案（`快捷键 [A-D / 1-4 · W读词 · S读句]` 与回退时的提示），彻底释放黄金阅读空间；
     * 将此前外置在卡片上方的配置功能深度内聚移入闪卡顶栏内小喇叭右侧：
       * **每组单词选择**：嵌入 `[每组 X 词]` 按钮，点击即可唤起每组词量调整弹窗；
       * **回退功能精简重构**：文案由原本的 `回退已学` 精简为直观易懂的 **`上一个`**，保留 `(N/2)` 上限保护（第 1 词或达上限时优雅置灰，最多可回退前 2 词）；
       * **返回进度无缝衔接**：处于回顾已学词状态时，旁边同步显眼展示 `[返回进度 →]` 按钮，点击或回车一秒切回最新学习进度。
  2. **外部状态指示栏与顶栏减负净化**：
     * **卡片上方状态行**（`第 X / Y 个新词`）：移除了嵌入其中的两个按钮，左侧保持清爽简洁的词量进度，右侧对应阶段状态标签，实现两端对齐与通透呼吸感；
     * **顶部模式工具栏去重**：在研习新词模式下隐藏顶栏中重复的 `每组: X 词` 按钮（因已直接常驻于闪卡顶栏最醒目位置），消除双重控件认知干扰。
  3. **工程质量与构建验证**：
     * 执行 `pnpm build`，全量 25 个静态与动态路由 100% 编译通过，0 TypeScript 错误；
     * 实机访问 `http://localhost:3000/cards` 返回 `HTTP/1.1 200 OK`，闪卡各控件排布严整协调，完全契合用户涂鸦示意图。

* **当前做到哪一步了（Current State Checkpoint）**：
  * 研习新词工作台完成精益重构，闪卡内部自洽内聚，外部状态栏纯净极简，交互层级清晰明确。

### Checkpoint 34：背新词大卡片底部边界对齐红线、高度缩减与防变形自适应重构
* **时间**：2026-09-13
* **执行角色**：UI/UX 设计师 & 前端主程
* **本阶段做了什么（What Was Done）**：
  1. **大卡片容器弹性高度收紧（彻底根除底部溢出与下边界贴底被裁切问题）**：
     * 响应用户实机截图标注（红线位于侧边栏用户信息卡片顶部基准线）：
     * 移除了大卡片父级容器与卡片本身的 `flex-1` 强制撑满特性，避免卡片在 1080p 笔记本（视口高度 ~750px）下强行被拉伸至视口最底端；
     * 剔除 `min-h-[390px] sm:min-h-[425px]` 固死高度，让卡片自然紧凑收拢，内边距优化为 `p-3.5 sm:p-4 md:p-5`（相比原 `p-7` 大幅节省纵向空间 24px+）；
     * 卡片底部边界精准收拢在用户指定的红线高度以上，距离视口底部留有充裕呼吸空间。
  2. **卡片内部核心构件等比例紧凑优化（保真不变形）**：
     * **单词标题与音标**：字号收拢为 `text-2xl sm:text-3xl md:text-4xl`，垂直边距由 `py-2 sm:py-2.5` 优化至 `py-1.5 sm:py-2`；
     * **四选一选项按钮**：高度收紧为 `min-h-[38px] sm:min-h-[42px]`，内边距为 `px-3 py-2 sm:py-2.5 rounded-xl`，字母角标调整为 `size-5.5 sm:size-6`，行间距缩小为 `my-1.5 sm:my-2 gap-2 sm:gap-2.5`；
     * **例句真题展开盒**：高度设为 `max-h-[72px] sm:max-h-[82px] p-2.5 sm:p-3 rounded-xl`，在展开解析时依然不会撑破视口或溢出红线；
     * **卡片底栏操作区**：`不认识/看详解` 与 `熟练斩词 (跳过)` 按钮优化为 `px-3 py-1.5 text-xs rounded-xl`，100% 完整显示且毫无遮挡。
  3. **工程质量与构建验证**：
     * 执行 `pnpm build`，全量 25 个静态与动态路由 100% 编译通过，0 TypeScript 错误；
     * 实机请求 `http://localhost:3000/cards` 返回 `HTTP/1.1 200 OK`，页面无滚动条、无视觉变形、底部控件完整呈现。

* **当前做到哪一步了（Current State Checkpoint）**：
  * 背新词大卡片已完全对齐红线基准高度，整卡精致典雅、信息布局紧凑饱满，在任何标准笔记本屏幕上均完美视口内呈现。

### Checkpoint 35：默写模块模式归属解耦、跨模直接流转与卡内状态聚合（零滚动视口防溢出）
* **时间**：2026-09-13
* **执行角色**：UI/UX 设计师 & 前端主程
* **本阶段做了什么（What Was Done）**：
  1. **模式归属解耦与跨模一键直达（消除强行拦截与多余手动退出）**：
     * 响应用户“默写模块应属于复习下模块，点击研习新词应直接退出默写并跳转”的明确反馈：
     * 封装 `exitChallenge` 回调，在顶部双模切换器中注入智能退出逻辑：在默写考核中点击「研习新词」按钮，系统自动触发 `exitChallenge()` 并一秒直达背新词工作台，无需再手动点击“退出默写”；
     * 点击「巩固复习」同样触发平滑退出并切回常规 3D 翻转卡片流，消除模式死锁；
     * 分段控制器样式优化：在复习默写进行时，「巩固复习」保持激活高亮状态，清晰向用户传达“默写属于复习链路下的阶段强化加固”之认知模型。
  2. **状态指示与退出控件移入词条卡内顶栏（1:1 落实涂鸦原型设计）**：
     * 彻底废除原本横跨在卡片上方的独立大型琥珀色横幅（`阶段强化 · 单词默写 ...`），瞬间节省 65px+ 垂直高度；
     * 将关键元数据与控制按钮深度收拢至默写卡片内部顶栏：
       * 左侧：`[⚡ 复习单词默写]` / `[新词拼写默写]` 琥珀胶囊徽章 + 记忆稳定性 `S = X.Xd`；
       * 右侧：`第 X / Y 词` 进度标牌 + `[Tab 提示 · Enter 提交]` 快捷键说明 + `[✕ 退出默写]` 紧凑按钮（支持快捷键 `Esc` 一键返回）；
  3. **击键动力学 HUD 嵌入与卡片高度紧凑自适应（彻底根除纵向滚动条）**：
     * 将外部 50px 的四格大 HUD 重构为卡内内联的 26px 极简数据微条（思考时间、犹豫状态、退格次数、提示字母）；
     * 单词字母槽位（Slots）由 `size-11` 优化至 `size-7 sm:size-8.5 rounded-lg`；
     * 挖空语境句添加 `max-h-[70px] sm:max-h-[80px]` 弹性限高防撑高；
     * 默写输入框精炼为 `h-10 sm:h-11`，整卡总高度收拢在 300px 左右，在 1080p 笔记本甚至更小视口下 100% 完整平铺，实现 **零滚动条（Zero-Scroll）**。
  4. **工程质量与构建验证**：
     * 执行 `pnpm tsc --noEmit`，TypeScript 0 报错；
     * 执行 `pnpm build`，全量 25 个静态与动态路由 100% 编译成功；
     * 实机请求 `http://localhost:3000/cards` 返回 `HTTP/1.1 200 OK`。

* **当前做到哪一步了（Current State Checkpoint）**：
  * 默写模块与复习/学习模式完成双向无缝打通，卡片外观全面对齐用户涂鸦设计，视口紧凑锁定、全屏无滚动。

### Checkpoint 36：默写卡片高度适度舒展（对齐红框基准）与输入文本清洗（严格仅保留纯字母）
* **时间**：2026-09-13
* **执行角色**：前端主程 & 交互体验架构师
* **本阶段做了什么（What Was Done）**：
  1. **默写卡片高度按红框适度舒展扩充（告别过度挤压与下方多余留白）**：
     * 响应用户关于“按照红色方框适度增加高度”的视觉优化诉求，针对此前紧凑收缩后下方留白过多问题进行舒展扩充：
     * 卡片主容器设定 `min-h-[480px] sm:min-h-[520px] md:min-h-[540px]`，内边距扩展为舒适优雅的 `p-5 sm:p-6 md:p-8 rounded-3xl`；
     * 内部元素层级纵向舒展：
       * **卡顶状态条**：内边距与间距适度扩大至 `pb-3 mb-3.5`；
       * **HUD 微条**：微调至 `p-2 sm:p-2.5 rounded-2xl mb-4`；
       * **释义与音标区**：词义文本升阶为 `text-xl sm:text-2xl md:text-3xl font-bold`，视觉重心更为突出；
       * **字母槽位（Slots）**：由狭窄的 `size-7/8` 扩充为大气的 `size-9 sm:size-10 md:size-11 rounded-xl font-mono text-base sm:text-xl`，间距微调至 `gap-2 sm:gap-2.5`；
       * **语境挖空填词盒**：内边距增至 `p-3 sm:p-4 rounded-2xl`，最高限高扩容至 `max-h-[110px] sm:max-h-[130px]`，消除此前双行英文例句即触发滚动条的局促感；
       * **输入交互区**：输入框扩增为 `h-12 sm:h-13 rounded-2xl text-lg sm:text-xl font-bold`，辅以 `gap-4 sm:gap-6` 的底部提示快捷栏，整卡完全填满红框区域且确保整页无多余纵向滚动条。
  2. **默写输入与判定文本全链路清洗（严格纯字母 `[a-z]`）**：
     * 响应用户“同时为默写后增加判断逻辑，对输入的文字要做清洗，只保留字母”的严格要求：
     * 封装导出 `sanitizeLettersOnly(text: string)` 工具方法，利用正则 `(text || "").toLowerCase().replace(/[^a-z]/g, "")` 过滤所有空格、标点符号、连字符、数字以及输入法临时残留字符；
     * **目标词基准清洗**：`const targetWord = sanitizeLettersOnly(card.lemma)`，确保单词槽位数量与目标比对字符均为纯字母，消除连字符或标点导致的不可输入槽位死锁；
     * **实时输入清洗**：在输入框 `onChange` 中即时清洗，用户录入或粘贴含有空格、标点、全角字符的内容时即刻洗净为纯小写英文字母；
     * **自动判定与手动提交清洗**：`triggerSubmit` 与动力学判定核心函数 `evaluateKeystrokeDynamics` 全面接入 `sanitizeLettersOnly`，严格保障拼写比对的纯净性与稳健性。
  3. **工程质量与构建验证**：
     * 执行 `pnpm tsc --noEmit`，TypeScript 0 错误；
     * 执行 `pnpm build`，全量 25 个静态与动态路由 100% 编译成功；
     * 实机请求 `http://localhost:3000/cards` 返回 `HTTP/1.1 200 OK`。

* **当前做到哪一步了（Current State Checkpoint）**：
  * 默写卡片高度已精准对齐用户涂鸦红框，卡片外观大气舒适，文字清洗过滤严密无死角，全功能就绪并正常运行中。

### Checkpoint 37：阅读库研读抽屉右侧滑入展开动效与左侧高斯模糊区点击一键退出
* **时间**：2026-09-13
* **执行角色**：前端动效架构师 & UI/UX 设计师
* **本阶段做了什么（What Was Done）**：
  1. **沉浸式研读器挂载与层级提升（React Portal 全屏顶层覆盖）**：
     * 响应用户“对于阅读库，增加一个从右侧展开的动画，然后点击左侧的模糊区域可以直接退出”的视觉交互需求；
     * 使用 `createPortal(..., document.body)` 将研读抽屉直接挂载到顶层 `document.body`，彻底隔离父容器滚动环境，消除层叠上下文（Stacking Context）可能引发的阴影或定位裁切；
     * 抽屉开启时自动锁定背景页面滚动（`document.body.style.overflow = "hidden"`），抽屉关闭时自动还原，防止双重滚动冲突。
  2. **从右侧展开的平滑入场与出场动画（Slide-over Drawer Transition）**：
     * **入场展开**：右侧抽屉容器注入 `animate-in slide-in-from-right duration-300 ease-out` 动效，从屏幕极右侧自然丝滑滑入视口；
     * **退场收起**：设计 `isClosing` 状态，当用户触发退出（点击模糊层、点击右上角关闭按钮或按键盘 `Esc`）时，抽屉执行 `animate-out slide-out-to-right duration-250 ease-in fill-mode-forwards` 平滑滑出屏幕后干净卸载。
  3. **左侧高斯模糊区域直接退出（Click-Outside to Exit）**：
     * 独立构建左侧遮罩层 `<div onClick={handleCloseReader} ... />`，采用 `bg-background/50 dark:bg-black/65 backdrop-blur-md cursor-pointer`；
     * 鼠标悬停左侧模糊区域时呈现 `cursor-pointer` 手型指针，点击任意左侧模糊视口即刻触发平滑退出；
     * 右侧抽屉面板内部绑定 `e.stopPropagation()` 阻止事件向上冒泡，保障正文阅读、双语划词查询、字体排版切换等操作不受点击穿透干扰；
     * 全局接入键盘 `Escape` 监听，按 `Esc` 键同样平滑收起抽屉。
  4. **工程质量与构建验证**：
     * 执行 `pnpm tsc --noEmit`，TypeScript 0 报错；
     * 执行 `pnpm build`，全量 25 个静态与动态路由 100% 编译成功；
     * 实机请求 `http://localhost:3000/reading` 返回 `HTTP/1.1 200 OK`。

* **当前做到哪一步了（Current State Checkpoint）**：
  * 外刊阅读库研读抽屉已完整支持右侧平滑滑入展开动效、左侧高斯模糊区域点击退出、键盘 Esc 快捷退出与背景锁焦保护，体验丝滑流畅。

### Checkpoint 38：巩固复习工作台控件卡内聚合（1:1 对齐涂鸦原型：移除冗余顶栏并迁移词量与默写按钮）
* **时间**：2026-09-13
* **执行角色**：前端主程 & UI/UX 设计师
* **本阶段做了什么（What Was Done）**：
  1. **顶栏冗余按钮彻底清理（对齐涂鸦首行红叉与框线指示）**：
     * 响应用户涂鸦截图中对于顶部模式切换器右侧区域的重构指导：
     * **移除冗余直接默单词按钮**：彻底删除原本位于顶部右侧的琥珀色 `[直接默单词 13]` 独立按钮（对应涂鸦中被打大红叉区域）；
     * **移除每组词量设置按钮**：将 `[每组: 5 词]` 从顶部工具栏移除，彻底解放顶部导航条，右侧仅保留极简纯净的 `[声学同步 · 自动播音 [开]]`；
  2. **副标题栏控件收敛至卡内（对齐涂鸦次行指示）**：
     * 删除副标题栏中卡片外的 `[直接默单词 D]` 按钮，使副标题行恢复为极简大气的 `第 X / Y 张复习卡片 · S = X.Xd` 与右侧 `间隔复习加固` 纯文字状态，消除视觉断层；
  3. **复习卡片顶栏右侧内聚「每组词量」与「直接默单词」功能（对齐涂鸦双箭头与右侧红叉指示）**：
     * 移除原先小喇叭右侧局促且冗余的静态说明文本 `[空格翻转 · D默单词 · W读词]`（对应涂鸦小红叉）；
     * 将两项高频控制控件优雅嵌入复习卡片顶栏右侧：
       * **小喇叭原声朗读**：`Volume2Icon` 原声音频播放器；
       * **每组词量设定**：`SlidersHorizontalIcon` + `每组 X 词`（点击弹出修改弹窗，绑定 `e.stopPropagation()` 防止触发卡片翻转）；
       * **直接默单词按钮**：`KeyboardIcon` + `直接默单词` + `D` 快捷键徽章（琥珀色高对比度设计，点击一键直达当前词默写挑战，绑定 `e.stopPropagation()`）；
     * 整体风格与“背新单词”卡片顶栏形成完美统一的设计语言，视觉重心更聚焦、层级更分明。
  4. **工程质量与构建验证**：
     * 执行 `pnpm tsc --noEmit`，TypeScript 0 报错；
     * 执行 `pnpm build`，全量 25 个静态与动态路由 100% 编译成功；
     * 实机请求 `http://localhost:3000/cards` 返回 `HTTP/1.1 200 OK`。

* **当前做到哪一步了（Current State Checkpoint）**：
  * 巩固复习工作台已 100% 完美落地用户涂鸦示意图中的所有位置迁移与删除改动，复习卡片顶栏与新词卡片全面对齐。

---
*日志实时记录与维护中*  
*文件路径：`c:\Users\JieZou\Desktop\AIC\开发日志.md`*








