# ── 语脉 · 影子跟读语音桥接服务 (Speech Bridge) ──────────────────────────────
# 依赖清单：Python 3.10+ / fastapi / uvicorn / faster-whisper / transformers
#           torch / torchaudio / phonemizer-fork / librosa / soundfile
# 安装：powershell -ExecutionPolicy Bypass -File scripts\setup-speech-bridge.ps1
# 启动：powershell -ExecutionPolicy Bypass -File scripts\start-speech-bridge.ps1

fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9
requests>=2.32
numpy>=1.24,<2
soundfile>=0.12
scipy>=1.11
librosa>=0.10.2

# ASR：faster-whisper (CTranslate2 后端)，提供词级时间戳
faster-whisper>=1.0.3
ctranslate2>=4.3

# 音素识别（发音评测）与文本→音素转换
transformers>=4.40,<4.47
torch==2.3.1
torchaudio==2.3.1
# 注意：必须安装 PyPI 上的 ``phonemizer``（不要用 phonemizer-fork）。
# transformers 通过发行版元数据检测依赖，fork 只提供同名模块而缺少
# ``phonemizer`` 这个 dist，导致 Wav2Vec2PhonemeCTCTokenizer 加载直接失败。
phonemizer>=3.3

# 可选：阿里 SenseVoice（中/粤/英/日/韩，中文场景更快）
# funasr>=1.1.3
# modelscope>=1.15
